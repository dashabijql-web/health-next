# HealthNext 产品设计与开发计划

## 1. 产品目标

HealthNext 是面向矿山职业健康与安全管理的全新平台。产品主线不是展示数据，而是让管理人员能够完成一条闭环：

```text
发现风险 -> 定位人员与区域 -> 查看健康证据 -> 判断处置优先级 -> 执行处置 -> 留下审计记录
```

产品需要同时满足三类体验：

- 日常管理：稳定、清晰、信息密度适中，适合长时间使用。
- 实时指挥：风险醒目、定位迅速、动作明确，适合大屏与值班场景。
- 视觉亮点：360° 人体健康画像和井下巷道态势地图具有记忆点，但所有视觉效果都必须服务真实数据和业务操作。

技术路线保持为 Vue 3 主平台，Three.js 承载人体 3D，OpenLayers 或定制 Canvas 承载二维井下 GIS；只有未来需要完整三维矿井漫游、仿真或 VR 时，才增加独立 Unity 场景。

## 2. 设计原则

1. 先完成所有页面的信息架构、线框和状态设计，再进入批量开发。
2. 高保真设计按关键业务流程分批完成和验证，不等待所有视觉稿一次性完工。
3. 一套数据只保留一个权威口径。页面不得自行用局部数组推算全局人数、预警数或处理率。
4. 正常、关注、警告、危急、离线、陈旧、无数据必须有稳定且全局一致的视觉语义。
5. 科技感来自空间、动效、实时状态和精确细节，不依赖满屏发光、装饰性粒子或大面积蓝色。
6. 表格、筛选和处置页优先保证效率；指挥中心、地图和人体页允许更强的沉浸式表现。
7. 所有页面必须同时设计加载、空数据、请求失败、部分数据、无权限和窄屏状态。
8. 3D、地图和图表只是表现层。人员、设备、位置、健康记录和事件状态由后端领域模型管理。

## 3. 产品信息架构

### 3.1 一级导航

| 产品域 | 页面 |
| --- | --- |
| 总览 | 工作台、健康监测总览 |
| 安全指挥 | 安全指挥中心、井下态势地图 |
| 健康监测 | 实时监控、心率、压力、血压、血氧、睡眠、趋势预警 |
| 人员健康 | 职工档案、职工健康画像、360° 人体体征、入井健康准入 |
| 预警处置 | 风险预警、待办事件、设备紧急事件、风险规则、处置记录 |
| 设备与数据 | 设备管理、手表控制、原始报文 |
| 报告与智能 | 报告中心、AI 健康助手 |
| 系统管理 | 用户、角色、部门、工种、系统设置 |

### 3.2 核心跨页对象

| 对象 | 唯一标识 | 主要入口 | 复用界面 |
| --- | --- | --- | --- |
| 人员 | employeeId | 搜索、名单、地图标记 | 人员快速详情抽屉 |
| 设备 | deviceId / IMEI | 人员绑定、设备列表 | 设备详情抽屉 |
| 健康读数 | recordId + measuredAt | 实时监控、专项分析 | 读数详情与趋势 |
| 风险事件 | incidentId | 指挥中心、预警列表、地图 | 事件处置抽屉 |
| 井下区域 | areaId | 地图、人员分布 | 区域详情面板 |
| 地图版本 | mapVersionId | 地图管理 | 图层与版本面板 |

事件处置抽屉和人员快速详情抽屉必须成为全平台公共组件。它们在指挥中心、地图、实时监控、人员画像和预警列表中保持相同内容与操作语义。

## 4. Figma 在项目中的作用

Figma Design 可以独立完成全部正式产品设计。Gemini、Grok 或其他生图工具只是可选的视觉探索助手，不是必需环节。已有明确审美方向时，可以跳过外部 AI 出图，直接在 Figma 中完成线框、组件、高保真页面和交互原型。

Figma 主要承担：

- 用 Frame 和 Auto Layout 固定布局规则，使页面在 1440、1920 和窄屏下都有明确行为。
- 用 Variables 管理颜色、字号、间距、圆角、阴影、密度和明暗主题。
- 用 Components 和 Variants 管理按钮、输入框、表格、状态标签、指标卡、抽屉、图例和地图工具。
- 用交互原型串联搜索人员、查看画像、地图定位、打开事件、完成处置等核心流程。
- 用 Dev Mode 提供尺寸、间距、颜色和资源信息，减少开发者凭截图猜测。
- 用评论和版本历史完成评审，不让聊天记录成为设计事实源。
- 保存 AI 效果图作为 Reference；正式界面必须在 Figma 中重建为可测量组件。

推荐采用以下两种工作方式之一：

| 工作方式 | 适用情况 | 流程 |
| --- | --- | --- |
| Figma 主导 | 已经清楚页面结构和视觉方向 | Figma 线框 -> 组件 -> 高保真 -> Prototype -> Dev Mode |
| AI 辅助探索 | 指挥中心、人体 3D、巷道地图等页面需要寻找视觉上限 | Gemini/Grok 生成参考方向 -> 选取可用元素 -> Figma 重建 -> Prototype -> Dev Mode |

日常表格、表单、后台管理、规则配置等页面直接在 Figma 设计即可。安全指挥中心、井下地图和 360° 人体页可以选择性使用 Gemini/Grok 发散构图、材质和灯光。AI 图片不能替代 Figma，因为图片没有可靠的组件状态、响应式规则、字段定义和可测量布局。

建议建立一个 Figma 文件，页面结构如下：

```text
00_Cover-And-Brief
01_Foundations
02_Components
03_App-Shell
10_Command-Center
20_Health-Monitor
30_Employee-Health
40_Incident-Management
50_Device-And-Admin
60_Reports-And-AI
90_Prototype-Flows
99_Archive
```

每个业务页面至少包含：`Desktop 1440`、`Wide 1920`、`Narrow 1024`，关键操作页额外包含 `Mobile 390`。平台以桌面端为主，移动端只保留查询、查看和轻量处置，不强行压缩完整大屏。

### 4.1 Figma 基础变量

| 类型 | 建议 |
| --- | --- |
| 背景 | Graphite 950 `#071018`、Graphite 900 `#0B151F` |
| 表面 | Graphite 850 `#101D28`、Graphite 800 `#162631` |
| 主文字 | `#EAF2F5` |
| 次文字 | `#91A5AD` |
| 强调 | Cyan `#36C5D8`，只用于交互和实时信号 |
| 正常 | Green `#35C58A` |
| 关注 | Amber `#E9AE52` |
| 危急 | Red `#EF6262` |
| 信息 | Blue `#5A8DEE` |
| 圆角 | 普通控件 4px，面板 6px，弹窗 8px |
| 间距 | 4、8、12、16、24、32 |
| 字体 | 中文使用 PingFang SC / Microsoft YaHei，数字使用 DIN Alternate 或等宽数字特性 |

### 4.2 必建组件

AppShell、Sidebar、Topbar、PageHeader、FilterBar、StatusTag、MetricTile、DataTable、Pagination、EmptyState、ErrorState、StaleState、PersonSummary、VitalReading、TrendChartFrame、MapToolbar、MapLegend、Timeline、IncidentDrawer、PersonDrawer、ConfirmDialog、AuditRecord、Skeleton。

每个组件至少建立 Default、Hover、Active、Disabled、Loading 和 Error 变体；状态组件额外建立 Normal、Attention、Warning、Critical、Offline、Stale 和 NoData 变体。

## 5. 可选的 AI 效果图生成方法

本节只在需要视觉探索时使用。不要让 Gemini 或 Grok 一次生成整套系统。先固定母提示词，再逐页生成。每个关键页面最多生成三个方向，只选有价值的构图或视觉元素进入 Figma：

1. `Operational`：工作效率优先。
2. `Immersive`：指挥中心和亮点页使用，空间感更强。
3. `Balanced`：默认方向，兼顾信息密度和视觉表现。

AI 图片中的中文、数值和图标只作为构图参考。文字、图标、表格、图表和控件必须在 Figma 中重新制作。

### 5.1 统一母提示词

将下面的母提示词放在每个页面提示词之前：

```text
为“HealthNext 矿山职业健康与安全管理平台”设计一张可实际开发的高保真网页 UI 效果图。直接展示 1440×900 桌面网页截图，不要展示笔记本电脑、手机模型或透视展示板。目标用户是矿山安全管理人员、职业健康管理人员和值班调度员。整体风格为克制、专业、精密的工业科技感：石墨黑与中性深灰为背景，青色表示实时信号，绿色表示正常，琥珀色表示关注，红色只表示危急，蓝色表示普通信息；避免全屏单一蓝色、紫色渐变、霓虹泛滥、装饰光球和赛博朋克游戏界面。左侧导航约 216px，顶部工具栏约 56px，内容区域信息密度适中，面板圆角不超过 6px。使用真实中文产品文案、清晰数字、规整表格、准确图表图例和专业线性图标。页面需要看起来能由 Vue 3、Element Plus、ECharts 和 Three.js 实现。不要营销页、不要巨型标题、不要卡片套卡片、不要无意义装饰、不要虚构医学结论。当前页面要求如下：
```

### 5.2 通用反向提示词

```text
排除：营销落地页、夸张英雄区、玻璃卡片堆叠、过度圆角、发光描边泛滥、紫蓝渐变背景、漂浮光球、随机英文乱码、重复菜单、透视变形、不可读小字、图表无坐标与单位、用平均数冒充个人风险、把高危体征写成 SOS、把未接入功能显示为成功。
```

## 6. 全页面效果图提示词

使用方式：`统一母提示词 + 对应页面提示词 + 通用反向提示词`。

### P01 登录页 `/login`

```text
设计平台登录页。以真实矿井调度与职业健康场景照片作为全屏背景，照片清晰但降低干扰，左上明确显示 HealthNext 和“矿山职业健康与安全管理平台”。登录表单宽约 380px，位于右侧安全区域，包含用户名、密码、记住登录状态、登录按钮和服务状态提示；不要注册、社交登录和宣传功能列表。画面应沉稳、安全、可信，首屏在 900px 高度内完整显示。
```

### P02 工作台 `/home`

```text
设计登录后的个人工作台。顶部显示班次、当前矿区、数据更新时间和全局人员搜索。首行是“我的待办、今日高危、待复核准入、离线设备”四项摘要；主体左侧为待办时间线与事件优先级，中间为今日健康态势和七日趋势，右侧为快捷入口与系统连接状态。突出“下一步该做什么”，避免做成领导大屏。提供加载失败但保留缓存数据的提示样式。
```

### P03 健康监测总览 `/health-monitor/dashboard`

```text
设计全矿健康监测总览。顶部提供日期范围、部门、工种、班次筛选。第一行展示在册人数、当前在线、体征覆盖、当前异常人数，异常主值写成“异常人数 / 覆盖人数”。主体包含各指标覆盖与异常分布、部门风险排名、七日异常趋势、设备采集质量和重点人员名单。图表必须有单位、样本数、更新时间和空数据语义，整体适合日常分析，不要与安全指挥中心重复。
```

### P04 安全指挥中心 `/safety-command/index`

```text
设计 16:9 沉浸式安全指挥中心。首屏无需普通页面标题，顶部紧凑展示矿区、班次、实时连接和全屏按钮。中央是井下态势小地图与事件定位；左侧展示预警总数、高危待办、未分派、已超时和重点风险人员；右侧是按严重度排序的实时事件流；底部展示部门风险、设备覆盖和处置时效趋势。点击事件应能进入统一事件处置抽屉。所有数字标明周期和更新时间，不显示无事件上下文的批量呼叫按钮。
```

### P05 井下态势地图 `/mine/map`

```text
设计专业的井下巷道 GIS 全屏页面。中央画布展示来自 CAD/Shapefile 的真实复杂巷道线网、井筒、水平、工作面和中文注记，使用平面工程坐标，不叠加城市道路底图。左上是地图名称、版本和实时连接；左侧为图层树；右侧为人员、设备、事件和区域详情面板；右上提供搜索、适应窗口、测距、图层、二维/轻量三维和全屏工具；底部显示坐标、比例尺与时间轴。地图叠加正常人员、离线设备、预警、SOS 四种标记，选中事件后高亮所在巷道并打开详情。视觉沉浸但不遮挡地图。
```

### P06 实时监控 `/health-monitor/real-time`

```text
设计高密度实时人员健康监控页。顶部显示在线窗口 15 分钟、体征新鲜度 5 分钟、状态筛选、指标筛选、部门和人员搜索。摘要展示在线人数、当前异常人数、数据陈旧人数和无数据人数。主体使用服务端分页表格，列出人员、部门、设备、在线状态、心率、血压、血氧、体温/压力、各指标采集时间和预警原因。右侧可展开紧凑事件栏。清楚区分 normal、warning、stale、no_data；刷新失败时显示“正在查看缓存数据”而不清空表格。
```

### P07 心率分析 `/health-monitor/heart-rate`

```text
设计心率专项分析页。顶部含日期范围、部门、工种和“当前异常”快捷筛选。摘要展示覆盖人数、异常人数、最低值、最高值和样本数。主体上方为带正常区间背景的心率趋势，下方左侧为各部门偏低与偏高记录数的分组柱状图，数值轴单位为“条”；下方右侧为服务端分页的异常人员名单。点击图表或名单打开人员详情，显示数据时间和阈值来源。
```

### P08 压力分析 `/health-monitor/pressure`

```text
设计心理压力/压力指数专项分析页。布局沿用心率分析模板，但指标语义使用压力等级分布、连续高压力时段、部门高压力记录和重点人员。不要使用虚构医学诊断或情绪画像。图表显示阈值来源、采集覆盖和样本量，列表可下钻到人员健康画像与原始记录。
```

### P09 血压分析 `/health-monitor/blood-pressure`

```text
设计血压专项分析页。摘要展示覆盖、异常人员、收缩压极值、舒张压极值。主图同时展示收缩压与舒张压趋势以及正常范围，避免双轴误导；部门图分别统计偏高和偏低记录；异常名单显示最近读数、测量时间、连续异常次数和处理状态。明确 mmHg 单位，不能把单次异常写成诊断结论。
```

### P10 血氧分析 `/health-monitor/blood-oxygen`

```text
设计血氧专项分析页。摘要展示覆盖人数、低血氧人数、最低有效值、无效/未佩戴读数数量。趋势图标明有效样本与缺失区间，分布图按业务阈值展示，异常名单显示人员、设备、最近有效读数和采集时间。95 的设备占位值或无效读数应显示为无效数据而不是正常血氧。
```

### P11 睡眠分析 `/health-monitor/sleep`

```text
设计睡眠分析页。顶部选择日期、部门和班次。展示昨夜上传覆盖、平均睡眠时长、达标人数和需关注人数；主体包含睡眠时长分布、深浅睡结构、七日趋势、部门上传情况和人员明细。空数据时说明“尚未收到睡眠数据”，不要生成虚假的睡眠评分或医学结论。
```

### P12 趋势预警 `/health-monitor/trend-warning`

```text
设计趋势预警页，强调“趋势风险”与“当前阈值越界”的区别。顶部提供指标、预测窗口、部门和风险等级筛选。主体左侧为趋势风险队列，显示预测方向、置信/证据说明、连续变化天数和当前是否越界；右侧是选中人员的原始时间序列、阈值线和预测区间；底部显示规则说明和审计记录。不要用 AI 语言制造确定性医学判断。
```

### P13 风险预警 `/health-monitor/risk-warning`

```text
设计健康风险分析与预警查询页。将 HEALTH_THRESHOLD、DEVICE_ALARM、TREND_WARNING 三类来源清晰区分，严重度作为独立字段。顶部筛选来源、事件代码、严重度、状态、部门和时间。摘要按来源和状态聚合，主体为可排序分页表格，点击行打开事件处置抽屉。SOS 只能出现在设备主动上报事件中。
```

### P14 职工档案 `/health-monitor/employee-archive`

```text
设计职工档案列表。顶部是姓名、工号、手机号、IMEI 搜索，以及部门、工种、在职状态和设备绑定筛选。主体采用表格而不是大卡片，显示人员身份、部门、工种、联系方式脱敏、设备绑定、最近在线、健康数据更新时间和当前风险。右侧操作包括查看画像、地图定位和编辑档案；支持批量导入但明确校验结果。
```

### P15 职工健康画像 `/health-monitor/employee-profile`

```text
设计权威的职工健康画像页。顶部是人员搜索和身份摘要。主体左侧展示当前体征，每个指标独立显示采集时间、新鲜度和状态；中部展示 7 日趋势和今日活动；右侧展示当前设备、联系处置入口和近 30 日预警轨迹。历史区包含日期范围与查询按钮，超过 7 日显示按日聚合和完整样本数。提供进入 360° 人体体征视图的醒目入口，但不要重复整页信息。
```

### P16 360° 人体体征 `/health-monitor/body-360`

```text
设计平台标志性的 360° 人体健康可视化页面。中央为可拖拽旋转和缩放的真实全身 GLB 人体线框模型，占据主要视野，不放在装饰卡片中；模型使用克制的青绿色扫描材质、局部扫描线和少量粒子。人体热点对应心率、血氧、体温、压力和血压，热点状态来自真实读数，选中后显示数值、单位、采集时间、阈值与最近趋势。左上提供人员搜索和身份切换，右侧是紧凑实时体征栏与风险事件，底部提供自动旋转、重置视角和减少动态。必须有模型加载、WebGL 不可用、低性能和无数据状态，不使用装饰性人体热区或虚构 ECG。
```

### P17 入井健康准入 `/health-monitor/mine-entry`

```text
设计班前入井健康准入页。顶部选择班次、部门、检查时间和准入状态。摘要展示应检、已检、准入、复检和禁止入井人数。主体左侧为人员检查队列，右侧为选中人员的身份、设备、最新体征、规则命中、复检记录和准入结论。结论必须明确由哪条规则和哪次数据产生，人工复核需要填写原因并记录审计。
```

### P18 报告中心 `/health-monitor/report-center`

```text
设计报告中心。顶部包含报告类型、对象、时间范围、生成状态和搜索。主体为报告任务表格，显示报告名称、覆盖范围、样本数、生成方式、创建人、状态和更新时间；右侧预览区展示报告摘要、数据截止时间和风险提示。支持生成、重新生成、预览、下载和失败重试，AI 报告明确标注辅助分析且不作为医学诊断。
```

### P19 手表原始报文 `/health-monitor/watch-raw`

```text
设计面向运维人员的手表原始报文页。顶部筛选 IMEI、协议代码、方向、解析状态和时间范围。主体是虚拟滚动或服务端分页日志表格，显示接收时间、设备、数据源、协议类型、原始报文摘要、解析结果和耗时；详情抽屉展示原始文本、结构化字段、处理链路和错误原因。使用等宽字体，支持复制和导出，敏感数据脱敏。
```

### P20 手表控制 `/health-monitor/watch-control`

```text
设计手表控制台。左侧为设备搜索和在线设备列表，中间为选中设备状态、人员绑定、电量、最近通讯与当前任务，右侧为允许的控制命令。立即测量按心率、血压、血氧、体温分开触发，清楚显示 AP 确认不等于测量结果；命令下发、确认、数据回传形成时间线。离线或未绑定状态禁用相关操作，所有操作需要确认和审计。
```

### P21 待办事件 `/alert-management/notifications`

```text
设计值班人员的待办事件页。顶部筛选严重度、来源、分派状态、SLA、部门和时间，显示我的待办、高危未确认、未分派和超时数量。主体为高密度分页表格，状态严格区分新建、已确认、已分派、处理中、已处理、已关闭和误报。行内只保留查看、确认和分派等高频动作，复杂处置进入统一事件抽屉。不要提供虚假的“已读”。
```

### P22 设备紧急事件 `/alert-management/sos`

```text
设计设备主动报警事件页。突出 SOS、跌倒、拆卸、低电等不同事件代码，严重度独立展示。顶部显示当前未处理设备报警、真正 SOS 数量和设备在线情况。主体左侧为事件列表，右侧为选中事件的人员、设备、地图位置、时间线和处置入口。页面必须避免把普通高危体征混入 SOS。
```

### P23 风险规则配置 `/alert-management/config`

```text
设计风险规则配置页。左侧按健康阈值、设备报警和趋势规则分组，右侧为规则编辑区域。健康规则包含指标、适用人群、上下限、持续时间、严重度和 SLA；设备报警不显示数值阈值，只设置启用、严重度、SLA 和通知策略；未接入的通知升级策略明确显示“未接入”。页面提供版本、启停、校验、发布和审计记录，发布前显示受影响人数估算和确认对话框。
```

### P24 处置记录 `/alert-management/records`

```text
设计已处理和已关闭事件的审计查询页。筛选来源、代码、严重度、处置结果、处理人、部门和时间。表格显示事件身份、发生时间、确认/分派/处理/关闭时间、SLA 是否超时、处理人和结论。详情时间线展示每次状态变化、备注、附件和外部动作结果；外部系统未配置时显示 NOT_CONFIGURED，不能显示已执行。
```

### P25 设备管理 `/admin/device-list`

```text
设计设备管理页。顶部支持 IMEI、型号、在线状态、绑定状态、电量和部门筛选。摘要展示设备总数、在线、离线、低电和未绑定。表格显示设备身份、绑定人员、最近在线、权威电量来源、固件、数据源和状态；详情抽屉包含通讯记录、健康采集状态、绑定历史和维护记录。批量绑定、导入和停用操作需要明确结果与失败明细。
```

### P26 用户管理 `/admin/user-list`

```text
设计后台用户管理页。顶部是账号、姓名、角色、部门和状态筛选。主体表格显示账号、姓名、角色、部门、状态、最近登录和创建时间；提供新增、编辑、重置密码、启停和查看审计。编辑使用右侧抽屉，表单分组清楚，危险操作需要二次确认。页面保持朴素高效，不使用大面积可视化。
```

### P27 角色管理 `/admin/role`

```text
设计角色与权限管理页。左侧角色列表显示成员数和状态，右侧分为基本信息、菜单权限、按钮权限和数据范围四个页签。权限树支持搜索、半选和展开收起；数据范围明确区分全部、本部门、本部门及下级和自定义部门。保存前显示权限变化摘要，避免用大量嵌套卡片。
```

### P28 部门管理 `/admin/department`

```text
设计部门组织管理页。左侧是可搜索的部门树，右侧是当前部门详情、负责人、人员数量、下级部门和状态。支持新增同级、新增下级、移动、编辑和停用；删除前展示关联人员、设备和规则影响。布局适合大型组织树，提供空节点和加载失败状态。
```

### P29 工种管理 `/admin/job-type`

```text
设计工种管理页。顶部按名称、风险等级和状态筛选，主体表格显示工种、风险分类、关联人数、适用健康规则、更新时间和状态。编辑抽屉包含名称、编码、风险标签、说明和规则关联；清楚展示停用对入井准入与风险规则的影响。
```

### P30 AI 健康助手 `/ai-chat/index`

```text
设计受控的职业健康 AI 助手。左侧为会话历史和新建会话，中间是对话区，右侧为本次分析引用的数据范围、时间范围、样本数和来源。首页提供“查询全矿概况、分析部门趋势、解释人员报告、生成报告草稿”等任务入口。回答中的数据结论可展开查看来源，涉及个人健康信息时显示权限边界，所有医学相关内容标记为辅助分析。不要做普通聊天机器人宣传页。
```

### P31 系统设置 `/admin/settings`

```text
设计系统设置页。左侧按基础信息、安全策略、实时监控、地图与资源、外部系统、通知和审计分组，右侧显示对应表单。敏感配置用密钥已设置状态代替明文；外部系统显示未配置、连接正常或连接失败，并提供测试连接。设置变更显示影响范围、版本和审计，危险配置需要确认。
```

### P32 事件处置抽屉（全局公共界面）

```text
设计从页面右侧打开的事件处置抽屉，宽度约 520px，背景页面仍可识别。顶部显示严重度、事件来源、代码、发生时间和当前状态；内容依次为人员与位置、健康证据或设备证据、责任人与 SLA、完整时间线、处置备注和附件。底部固定操作区根据状态显示确认、分派、开始处理、处理完成、误报和关闭。事件定位键、数据来源和更新时间可追溯，任何外部动作都显示真实结果。
```

### P33 人员快速详情抽屉（全局公共界面）

```text
设计从页面右侧打开的人员快速详情抽屉，宽度约 480px。顶部显示姓名、工号、部门、工种、联系方式和当前风险；随后显示绑定手表、Netty/连接在线状态、最新体征及各自采集时间、当前未处理事件和井下位置。底部提供打开完整画像、地图定位、文字消息和单人语音；只有已绑定且在线设备才启用下发，管理端没有“发送 SOS”操作。
```

### P34 全局异常、空态与无权限状态

```text
设计同一产品中的六种状态样板：首次加载骨架、业务空数据、筛选无结果、部分数据可用、请求失败但缓存仍可查看、无权限。状态区域应嵌入正常页面布局，提供清楚原因、数据时间和可执行动作；使用简洁图标，不使用大型插画或轻松娱乐化文案。另展示 404 和系统维护全页状态。
```

## 7. 关键用户流程

### 7.1 风险处置主流程

```text
安全指挥中心看到高危事件
-> 打开事件处置抽屉
-> 查看人员、体征证据和井下位置
-> 跳转巷道地图定位
-> 联系人员或分派责任人
-> 记录处理结果
-> 服务端返回最终状态
-> 指挥中心、地图、人员画像同步刷新
```

### 7.2 人员健康主流程

```text
全局搜索人员
-> 打开人员快速详情
-> 进入健康画像
-> 查看当前体征与历史趋势
-> 进入 360° 人体体征视图
-> 发现已有未处理事件
-> 打开事件处置抽屉
```

### 7.3 入井准入主流程

```text
选择班次
-> 查看待检查人员
-> 获取最新有效体征
-> 规则给出准入/复检/禁止建议
-> 人工复核并填写原因
-> 生成不可篡改的审计记录
```

## 8. 推荐技术架构

### 8.1 前端

- Vue 3、Vite、TypeScript、Pinia、Vue Router。
- Element Plus 按需导入；图标也按需注册，避免当前全量包继续膨胀。
- ECharts 用于统计图表，所有图表通过统一主题和容器组件创建。
- Three.js 只存在于 `modules/human-body` 等场景模块，通过明确的 mount、resize、update、dispose 接口接入 Vue。
- 井下二维 GIS 优先使用 OpenLayers 的自定义工程坐标系和矢量图层；当前 Canvas 原型作为渲染与交互验证依据。
- SSE 用于服务端单向实时更新；只有设备控制或协同场景确需双向通讯时使用 WebSocket。
- API 类型由 OpenAPI 生成或至少由共享 schema 校验，避免前后端手写字段逐渐漂移。
- 按业务域拆包，Three.js、地图、ECharts、Element Plus 独立异步 chunk。

推荐目录：

```text
web/src/
├── app/                 # 启动、路由、布局、权限
├── modules/             # 按业务域组织页面与服务
│   ├── command-center/
│   ├── mine-map/
│   ├── health-monitor/
│   ├── employee-health/
│   ├── incidents/
│   ├── devices/
│   ├── reports/
│   └── admin/
└── shared/              # 设计系统、请求、类型、通用组件
```

### 8.2 后端

- 保持 Spring Boot 模块化单体，当前阶段不要拆微服务。
- 按业务域组织 `auth`、`employee`、`device`、`health`、`incident`、`mine`、`report`、`admin`。
- 使用 DTO 和明确返回类型，逐步替换 `Map<String, Object>`。
- 引入 Jakarta Validation，错误响应包含稳定错误码、字段错误和 traceId。
- 正式认证接入用户、角色、权限和数据范围；本地单账号只保留开发 profile。
- 数据库迁移使用 Flyway，任何表结构变化必须由版本脚本完成。
- Redis 在实时状态、限流或队列需求明确后再引入，不因为旧系统使用而默认复制。
- 原始手表协议通过独立 adapter 接入领域服务，协议解析与健康业务解耦。
- 对象存储用于 GLB 模型、地图版本、报告和处置附件，不将大型资源打进主应用包。

### 8.3 数据库与数据迁移方案

#### 目标架构

HealthNext 当前脚手架没有数据库。正式开发建议采用以下组合：

| 组件 | 选择 | 职责 |
| --- | --- | --- |
| 主数据库 | PostgreSQL + PostGIS | 用户、组织、人员、设备、体征、事件、规则、井下地图与空间查询 |
| 数据库迁移 | Flyway | 建表、索引、约束、种子配置和版本升级 |
| 数据访问 | jOOQ 或 MyBatis（二选一后固定） | 类型安全查询、分页、统计和批量写入 |
| 缓存/短期状态 | Redis，按需求引入 | 设备在线状态、短期体征快照、限流和异步缓冲 |
| 对象存储 | S3 兼容存储或 MinIO | GLB 模型、DWG/Shapefile 原件、地图版本、报告和附件 |

首选 PostgreSQL + PostGIS 的原因：

- 普通事务数据和井下 GIS 数据可以在同一套事务、备份和权限体系中管理。
- PostGIS 能存储巷道线、区域面、井筒点和人员坐标，并提供空间索引、点在线附近、人员是否位于区域内等查询。
- PostgreSQL 原生支持声明式分区，足以承担第一阶段的健康测量与位置历史数据。
- 后续需要更强时序能力时，可以基于实际数据量评估 TimescaleDB，不必在第一版提前增加运维复杂度。

数据库方案固定为 PostgreSQL + PostGIS，代码和部署配置只维护这一套数据库方言与连接。Redis 即使在后续引入，也只承载缓存、短期在线状态和异步缓冲，不作为第二业务数据库或权威数据源。

#### 数据分层

```text
主数据：用户、角色、部门、工种、人员、设备、设备绑定
业务流水：健康测量、设备状态、人员位置、风险事件、处置时间线
规则配置：指标定义、阈值规则、趋势规则、SLA、通知策略
空间数据：地图版本、图层、巷道、井筒、工作面、区域、注记
资源数据：模型、CAD/GIS 原件、报告、处置附件
审计数据：登录、配置变更、规则发布、设备控制、事件处置
```

健康测量建议按 `measured_at` 做月度原生分区，人员位置历史按实际写入量选择按日或按月分区。所有表使用稳定主键，设备协议中的 IMEI、员工工号和外部 warningId 作为业务键或唯一约束，不直接承担跨系统主键职责。

#### 可选的旧数据离线导入

HealthNext 运行时只连接 PostgreSQL + PostGIS，不实现双数据源、数据源切换、旧库查询或新旧双写。若上线前决定保留部分旧数据，采用一次性的离线导出与导入：

```text
从旧系统导出脱敏的 CSV/Parquet 文件
-> 建立旧字段到新领域字段的映射
-> 离线清洗与校验
-> 先迁主数据，再迁历史流水和事件
-> 对每批数据核对数量、时间范围、空值、重复值和抽样内容
-> 导入 PostgreSQL
-> 生成导入批次和校验报告
-> HealthNext 上线后只读写 PostgreSQL
```

旧数据库不是 HealthNext 的运行依赖。离线导入工具只读取导出文件，必须记录批次、来源说明、时间范围、成功数、失败数和校验摘要，失败记录可单独重跑；导入完成后不在应用中保留旧数据库连接配置。

历史体征是否全部迁移，应根据法规、审计和业务查询周期决定。至少迁移人员、组织、设备绑定、有效风险规则、未关闭事件和需要在线查询的历史窗口；更老的数据可以进入归档库，但必须保留可检索路径。

#### 井下地图入库

当前 `mine-map.json` 原型包含 6461 条线、21059 个顶点和 112 条注记。正式导入流程应保留原始 DWG/Shapefile，并生成可追溯的地图版本：

```text
上传原始文件
-> 校验坐标系、单位和图层
-> 转换为 PostGIS geometry
-> 生成简化级别和空间索引
-> 人工检查井筒、水平、工作面和注记
-> 发布新地图版本
-> 前端按视口和缩放级别读取矢量要素
```

人员位置、设备和事件只引用地图版本与工程坐标，不直接修改巷道基础图层。地图升级后通过版本映射或坐标校验处理历史轨迹。

#### 数据安全与备份

- 健康信息和联系方式按敏感数据处理；列表默认脱敏，明文查看需要权限并写审计。
- 数据库账号按应用运行、迁移和只读分析分离，生产凭证只来自密钥管理或环境变量。
- 每日备份并持续归档 WAL，定期执行恢复演练；对象存储启用版本和生命周期策略。
- 删除人员时优先停用和匿名化，健康流水、处置记录和审计记录按法规与组织政策保留。

### 8.4 核心领域表草案

```text
employee, department, job_type
device, device_binding, device_status
health_measurement, health_metric_definition
incident, incident_evidence, incident_assignment, incident_timeline
risk_rule, risk_rule_version
mine_map_version, mine_layer, mine_feature, mine_area
person_location, location_history
report_task, report_artifact
sys_user, sys_role, sys_permission, audit_log
```

具体字段、约束和索引在页面字段与 API 契约冻结后设计。健康测量使用 PostgreSQL 原生分区，不复制旧项目按月动态表名、视图和存储过程的实现。

## 9. 分阶段执行计划

### 阶段 0：产品范围冻结，3-5 个工作日

产出：一级导航、角色清单、页面清单、术语表、核心对象、主流程、MVP 范围、数据库选型和明确不做项。

完成门槛：同一个概念只有一个名称；每个页面都有目标用户和首要任务；两个亮点页均能解释其业务价值。

### 阶段 1：Figma 设计系统，5-8 个工作日

产出：颜色、字号、间距、状态语义、AppShell、表单、表格、指标、图表框架、抽屉和空态组件。

完成门槛：至少用工作台、实时监控和用户管理验证同一套组件能覆盖大屏、分析页和后台页。

### 阶段 2：所有页面低保真设计，10-15 个工作日

按本计划 P01-P34 建立线框。先画信息层级、筛选、内容、动作和状态，不处理精细视觉。

完成门槛：所有页面有 Desktop 线框；关键流程能在 Figma Prototype 中走通；加载、空态、失败和无权限均有位置。

### 阶段 3：关键页面高保真，10-15 个工作日

优先顺序：AppShell -> 安全指挥中心 -> 巷道地图 -> 实时监控 -> 健康画像 -> 360° 人体 -> 事件抽屉 -> 入井准入。

完成门槛：1440 和 1920 布局稳定；关键交互有原型；地图和人体页使用真实资产截图验证构图；业务方确认主流程。

### 阶段 4：工程底座，7-10 个工作日

前端完成模块路由、权限、请求层、错误处理、设计变量、按需加载和测试框架。后端完成领域包、PostgreSQL/PostGIS、Flyway、验证、错误码、OpenAPI、审计基础和用户权限模型。

完成门槛：登录、权限拒绝、分页查询、统一错误、空态和审计形成一条可测试链路；首屏不再全量打包 UI 与图形库。

### 阶段 5：第一条完整业务链，15-20 个工作日

实现人员、设备、事件三个最小领域，以及工作台、待办事件、人员抽屉、事件抽屉和基础权限。

完成门槛：可以从事件找到人员和设备，完成确认、分派、处理和关闭，刷新后状态来自服务端且审计完整。

### 阶段 6：两项视觉亮点产品化，15-25 个工作日

巷道地图：地图版本、基础图层、搜索、人员/设备/事件图层、选中定位和事件下钻。

360° 人体：模型资源管理、场景组件、热点配置、人员切换、体征绑定、异常状态、低性能模式和资源释放。

完成门槛：视觉效果与真实业务数据连接；在目标办公电脑上稳定运行；路由切换无明显内存增长；无 WebGL 时仍能查看数据。

### 阶段 7：健康监测与准入，20-30 个工作日

实现实时监控、专项指标分析、历史趋势、健康画像、风险规则、趋势预警和入井准入。

完成门槛：时间、阈值、覆盖人数、异常人数和数据新鲜度口径一致；任何页面都不将无效数据解释为正常。

### 阶段 8：报告、AI、设备运维与后台，15-25 个工作日

实现报告中心、AI 助手、设备管理、手表控制、原始报文、用户、角色、部门、工种和设置。

完成门槛：AI 结论可追溯到数据范围；设备操作有确认和审计；权限和数据范围在后端生效。

### 阶段 9：上线准备，10-15 个工作日

执行性能、权限、数据隔离、备份恢复、浏览器兼容、可观测性、可访问性和视觉回归验收，准备灰度和回滚方案。

完成门槛：核心流程端到端通过；地图与 3D 有性能基线；错误和外部系统未配置状态不会伪装成功。

## 10. 设计与开发并行规则

可以在所有页面线框完成后开始工程底座和第一条业务链，不需要等待所有高保真页面完成。每个功能进入开发前必须满足 Definition of Ready：

- Figma 页面与交互已评审。
- 正常、加载、空态、失败、部分数据和无权限状态齐全。
- 字段、单位、时间范围、刷新规则和权威数据源明确。
- API 请求、响应和错误码已经确定。
- 页面可执行动作与权限、审计要求明确。
- 1440、1920、1024 的布局行为明确。

开发完成需要满足 Definition of Done：

- 实现与 Figma 关键尺寸和信息层级一致。
- 接入真实接口或明确标注的 mock adapter，没有散落在组件中的演示数据。
- 服务端分页、权限和业务状态真实生效。
- 自动化测试覆盖关键状态变化和数据口径。
- 完成桌面与窄屏截图检查，无重叠、截断和不可达操作。
- 地图、图表和 Three.js 组件在卸载时释放资源。
- 构建、类型检查、后端测试和差异检查通过。

## 11. 版本范围建议

### MVP

登录与权限、工作台、人员档案、设备管理、实时监控、待办事件、统一人员/事件抽屉、基础巷道地图、基础健康画像。

### V1

安全指挥中心、完整巷道业务图层、360° 人体、专项指标分析、风险规则、处置记录、入井准入。

### V1.5

趋势预警、睡眠、报告中心、手表控制、原始报文、AI 助手和系统集成。

对一名全职开发者配合 AI 工具，达到可试运行的 MVP 通常需要约 4-6 个月；做到完整、稳定且视觉精细的 V1，建议按 6-9 个月规划。两名前后端开发加一名兼职设计/产品角色，可以显著压缩到约 3-5 个月，但地图数据治理、真实设备联调和业务验收仍是关键路径。

## 12. 立即执行的下一批任务

1. 在 Figma 建立上述文件结构和基础 Variables。
2. 在 Figma 直接设计 AppShell 和一个日常管理页；需要寻找视觉方向时，再用 P04、P05、P15、P16 生成参考图。
3. 用 P06、P21、P26 验证同一视觉语言能否覆盖实时页、处置页和后台页。
4. 在 Figma 重建选中的页面，不直接把 AI 图片切图开发。
5. 画出风险处置、人员健康和入井准入三条可点击原型。
6. 冻结 PostgreSQL + PostGIS 数据库方案以及第一版设计系统后，再修改 HealthNext 工程底座和模块目录。
