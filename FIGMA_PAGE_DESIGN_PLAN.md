# HealthNext 页面设计计划（通俗版）

人体展示默认使用「沉浸人体」：`/health-monitor/body-360-immersive`，实现为 `web/src/modules/human-body/Body360ImmersivePage.vue`。原「360° 人体」`/health-monitor/body-360` 保留为非默认页面，不再作为新开发和视觉验收基准；人员档案、事件详情和健康画像的人体入口应进入沉浸人体，并携带当前人员上下文。

这份计划按页面说明：给谁用、能做什么、点哪里，以及怎样算设计完整。31 个业务页面全部保留，编号仍为 F01–F31。

数据库已经确定使用 Oracle，后台工作见 [后台实施计划](./ENGINEERING_IMPLEMENTATION_PLAN.md)，总体顺序见 [总体开发计划](./PRODUCT_DESIGN_PLAN.md)。这里的说明是设计要求，不代表页面已经做完或已接真实数据。

## 1. 怎么看这份文件

先看公共要求，再找到具体页面。每页都有“用来做什么”“怎么操作”“检查什么”。标题后的路径是开发人员定位页面用的地址，可以跳过不看。

“详情侧栏”就是点人或事件后，从页面右侧打开的详情区域，也叫抽屉。“Figma”是可选的设计工具。标准列表页当前默认复用已有代码并由 Grok Build 实现；展示型页面才用生图确定外观，再由编程 AI 实现。文件名保留 FIGMA 只是为了兼容已有链接。

每页后面可展开的提示词保留原有详细要求。生图时提取其中的布局、文字和状态，按下方通用生图说明出图；创建 Frame、Variables、Prototype 等指令只在选择 Figma 时使用。准确业务文案和操作以正文及页面材料为准。

## 2. 先设计什么

1. 先完成下方 D0–D3 样板流程：用 F18 待办事件和 C01 事件详情确定菜单、按钮、表格、颜色和字体，再复用到其他页面。
2. 优先完成待办事件，让发现异常、查看证据、确认、处理、关闭和更新列表能完整演示。
3. 补登录、工作台、总览与各主要入口。
4. 补专项分析与人员档案，让实时监控、个人健康页和人体页中的按钮都有去处。
5. 完善地图定位、设备详情、报警、控制和通信排查。
6. 完成规则、处理记录、报告、AI、用户权限和系统设置。
7. 最后逐条检查第 7 节的五个操作过程，包括返回后还记得刚才查什么。

## 3. 所有页面共同遵守的要求

### 看得清楚

统一使用深灰工业风，青色表示实时信息和当前操作，绿色表示正常，琥珀色表示关注，红色表示危急，蓝色表示普通信息。状态同时有文字或图标，不只靠颜色。

每个数字说明单位、时间范围或更新时间，统计注明覆盖人数或样本数。按钮和表格保持相同用法；图表有标题、单位、图例和无数据提示。

### 大小屏幕都能用

检查 1440×900、1920×1080 和 1024×768 布局。重要查询与轻量操作还要检查 390×844 手机布局。大地图、指挥中心和人体页在手机提供摘要，保留大屏查看入口。

窄屏可以把次要表格列收进详情，但主要信息和按钮必须能访问。

### 正常和出错时都画出来

每页都有正常、加载中、没有业务数据、筛选无结果和失败的设计。实时页面还要有数据过旧、连接断开；操作页要有无权限、按钮不可用、确认和执行中的设计。

有上次数据时刷新失败，可以保留并注明时间。部分数据缺失就明确标记，不能用零或模拟数据填上。

### 跳转后还知道自己在看什么

点人员打开统一人员详情，点事件打开统一事件详情，点设备打开统一设备详情。快速查看用侧栏，复杂分析进入专门页面。

同一时间只开一个主要详情侧栏，人员和事件之间切换时保留返回入口。进入另一个页面，要带上人员、事件、日期和筛选条件，并让这些条件可见；返回时恢复页码和滚动位置。

点图表或统计数字进入名单后，能看出来自哪里、用了哪些条件。清除条件后留在当前页面。浏览器后退要可用，刷新或分享链接后应能恢复所选内容。

### 操作结果要真实

确认、分派、处理、关闭、误报、绑定设备和发布规则后，重新读取服务器结果，同时更新详情、列表、数字和地图。

停用账号、停用设备、解除绑定、发布规则和关闭事件时，说明对象和影响，再让人确认。外部呼叫、广播和设备控制如实显示正在发送、已确认、失败或未配置。

## 从生图到代码：这一套直接用于开工

### A. 先固定样板和交付材料

第一张用 F18 待办事件，第二张用同一页打开 C01 事件详情，第三张用同一页的无待办状态。先做这三张，表格、侧栏和空数据都验证过再扩展。

每页的材料包含七部分，已有文字说明直接复用，不让生图工具自行决定业务：

- 页面身份：F 编号、名称、目标路径、用途、使用角色和本批范围。
- 视觉输入：选定原图、对应工具与提示词、参考图、尺寸、版本和修改意见。
- 准确内容：标题、字段名、单位、按钮文案、测试用文字和演示数据。
- 操作说明：点哪里、输入什么、允许谁操作、成功后哪里更新、失败时怎么办。
- 数据说明：字段名称、是否可以为空、测量与刷新时间、分页总数和真实接口或演示来源。
- 状态说明：正常、加载、无数据、无结果、部分数据、过旧、失败、无权限和执行中如何呈现。
- 验收证据：设计图与网页截图、使用的窗口尺寸、检查步骤与结果、工具/代码版本、尚未完成项。

实施时建议放在 `design-delivery/F18/v01/`：`brief.json` 保存页面材料，`copy.json` 保存准确文案，`fixtures.json` 保存演示样本，`source/` 保存选定图和提示词 TXT，`actual/` 保存网页截图，`acceptance.json` 保存检查结果。全平台样式保存在 `design-delivery/shared/style-v01.json`。这些是约定的未来产物，本次不创建占位文件或假验收结果，不新增 Markdown 文档。

每次改选定图产生新版本，并让验收记录指向同一版本。接口说明保存为 JSON/YAML 或代码类型；账号密码和真实个人健康资料不能放进生图材料。

### A1. Gemini 和 Grok 怎么分工

标准列表页（值班名单、预警记录、待办事件、人员档案）直接由 Grok Build 基于已有代码实现，不需要生图探索。Gemini 只用于指挥中心、地图、三维人体等需要视觉探索的页面。列表页使用集中管理的 mock 数据，完成布局、表格、筛选、抽屉、按钮状态和各种页面状态；不连接 Oracle，不实现正式权限，不发送真实通知或设备控制，也不自行决定健康阈值。

Grok 的工作是接收 Gemini 的样板和交接材料，保留已经确认的视觉结构，整理组件和数据访问层，补齐服务端分页、权限、错误处理、并发冲突和真实接口。真实接口接通后，Grok 负责用服务器返回覆盖 mock 计数、时间、状态和允许动作。

两次交接至少包含：选定图版本、`brief.json`、`copy.json`、`fixtures.json`、按钮和状态说明、样板截图、构建结果以及尚未完成项。Gemini 的样板通过只表示“页面可操作”；Grok 接通真实接口并完成业务流程后，才可以标记“功能完成”。

### B. 生图的统一要求

以 1440×900 的桌面布局为样板，直接显示平面网页，不加电脑外壳、透视、手机模型或营销标题。工具不能精确输出该像素时，选择最接近的 16:10 画面，保留原尺寸并按比例换算，不能拉伸后拿错比例验收。

固定侧栏约 216px、顶栏约 56px、内容间距 24px、正文约 14px、表格行高约 44px、详情侧栏约 520px；小屏按后文调整。配色沿用 F00，最终以用户选定的样板记录为准。

每次只生成一页的一个状态。后续页面都附上同一张选定参考图，要求保留公共菜单、颜色、文字大小和按钮形状。关键文字以 copy.json 为准，图片中错字在代码里纠正；截图中的演示数字不能替代统计逻辑。

用户可选 GPT Image 2.5、Grok 或 Gemini。第一轮最多先看三个方向，选定后沿用一个主工具和一套参考；另一个工具只用于补充或比较。记录实际使用的型号，不预设所有账号都能访问同一型号。

### C. 可以直接用于第一张图的提示词

把下面内容交给所选生图工具；后面再附本文件 F18 的页面要求。第二、三张图必须附上第一张选定图作为参考。

```text
为 HealthNext 矿山职业健康与安全管理平台生成“待办事件”桌面网页设计图。目标布局 1440×900，平面正视图，只展示网页，不要电脑模型、透视、海报或拼成多页。
使用深灰背景、青色交互、绿色正常、琥珀色关注、红色危急；专业清晰，表格信息密度适中。左侧约 216px 菜单，顶部约 56px 工具栏。保留菜单：总览、安全指挥、健康监测、人员健康、预警处置、设备与数据、报告与智能、系统管理；当前选中“预警处置 / 待办事件”。
页面标题“待办事件”。四项摘要依次是“我的待办 2”“高危未确认 1”“未分派 2”“已超时 1”。明确标记“演示数据”。
筛选：人员搜索、部门、事件来源、严重程度、处理状态、时间范围；按钮“查询”“重置”“刷新”。
表格列：人员、部门、事件、来源、严重程度、发生时间、责任人、处理时限、状态、操作。展示四条虚构演示事件：演示职工甲心率异常，演示职工乙设备低电，演示职工丙血氧异常，演示职工丁血压异常。不要把健康异常画成 SOS。未知位置和负责人用明确文字表示，不补虚构真实人员。
每行操作“查看”，允许的行另有“确认”“分派”；底部真实样式的分页区域。不要多余图表和大装饰。
第一张只画列表默认状态。随后基于同一参考图，分别画“打开第一条事件详情”和“无待办”两个状态，每次只输出一张。
布局和风格以图为准，准确文字和数字在代码里以提供的页面材料为准。
```

详情图要求：保留背景页面，右侧打开约 520px 详情，展示事件、人员、来源与严重度、时间、测量依据、恢复情况、处理情况、责任人、处理经过、备注和按权限允许的按钮。底部操作区可见，中间内容可滚动。无待办图要求：保留标题、筛选和摘要，显示“当前没有待办事件”和刷新入口。

### D. skill 怎样选，失败了怎么办

首选试用 [UI UX Pro Max](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill) 作为样式和交互检查辅助，可让 Gemini 或其他支持图片输入的工具参考设计图生成 Vue 样板。它提供设计规则，不是独立的精确图片转换器。实施时检查安装入口、代码和许可，记录使用的提交版本；在当前环境能否完整运行，以样板试做结果为准。

[截图转代码项目 screenshot-to-code](https://github.com/abi/screenshot-to-code) 作为备选比较工具。它是独立应用，仓库列出的 Vue 输出使用 Tailwind，不能直接当成已适配本项目 Element Plus 的 skill。它产生的结果需在隔离试验目录检查、改写后再接入现有项目，不把它的服务带入生产系统。

试用只用同一组 F18 图片和材料。检查五项：能否遵守 Vue 3/TypeScript/Element Plus；能否复用布局；文字和表格能否编辑；筛选侧栏是否正常；是否能构建并按截图修正。记录耗时、修改次数和不满足项。

先做一轮生成和最多两轮集中修正来评估工具。仍然换技术栈、整页铺图片、生成难维护的绝对定位或破坏公共布局，就停止采用该输出，回到编程 AI 直接按图逐组件实现。此上限只控制选工具的试验；最终页面仍需修到验收通过。无需为了找高星工具不断试新仓库。

星数只用于发现候选，不代表质量已通过。本次只完善计划，不安装、执行第三方 skill 或宣称已试用通过。

### E. 交给编程 AI 的固定要求

```text
请按已确认的视觉样板、现有列表页代码、选定图片、brief.json、copy.json、fixtures.json 和当前页面要求，继续实现 HealthNext 的 F18 待办事件与 C01 事件详情。
先检查当前工作树和现有页面，保留已有改动。使用 Vue 3、TypeScript、Element Plus 和现有样式方式，复用现有布局、路由守卫与请求封装，不另建 React/Next.js 项目，不自行引入 Tailwind 或另一套组件库。
准确文字、数据含义、权限和状态以页面材料为准；已选定图片决定外观。发生冲突时记录并修正，不由图片猜业务。
先拆公共菜单、摘要、筛选、事件表格、分页、事件详情和处理表单，再实现页面。不把整张图作为网页背景，不把按钮和表格切成图片，不为每一行使用固定绝对坐标。
样板期的演示数据集中管理、明确标记，并通过可替换的数据层使用；不写死在每个组件里，不发送真实通知或控制命令。真实接口阶段复用 web/src/utils/request.ts。
逐项实现材料中的正常、加载、无数据、无结果、部分数据、过旧、失败、无权限、重复点击与并发冲突；未接通的跳转显示明确说明，不假装进入一个不存在的页面。
运行项目，在规定尺寸截图与选定图对照，修正差异，检查筛选、翻页、详情、处理和返回；执行项目已有构建命令，并记录实际结果。未完成的项列清楚，不用一句“还原完成”代替检查。
```

Element Plus 优先用于表格、表单、分页、选择框和侧栏。公共颜色和间距集中管理，后续页复用样板。图表应是真实图表组件，地图要能缩放定位，三维人体用模型与交互实现；图中的静态图案不能冒充这些功能。

### F. F18 的首批内容已经这样定

页面路径规划为 `/alert-management/notifications`，目标使用现有布局。当前已有预警记录入口是 `/health-monitor/warnings`，不能覆盖用户正在做的记录页；新增入口前检查路由并区分“待办”和“记录”。本节路径和字段是开发约定，尚不表示接口和页面已实现。

样板只做四条固定的虚构事件，所有人名以“演示”开头，固定一份测试时钟。计数从这份样本按同一规则得到：新建、已确认、已分派、处理中、处理完成但未关闭都属于待办；关闭和误报不属于待办。我的待办按当前处理人计算，高危未确认指高危且新建，未分派指无负责人，超时按处理截止时间比较测试时钟。

四条起始样本：甲为高危新事件、未分派、未超时；乙为普通设备低电、新事件、未分派、未超时；丙为高危血氧事件、处理中、分给当前用户、已超时；丁为关注级血压事件、处理完成待关闭、分给当前用户、未超时。对应初始摘要为我的待办 2、高危未确认 1、未分派 2、已超时 1，总待办 4。生图与代码使用同一份样本和计数，新增或改动样本后同步更新摘要。

显示字段：事件编号、人员编号和姓名、部门、设备编号、位置或缺失原因、来源、事件名称、严重度、发生时间、指标值与单位及采集时间、恢复情况、处理状态、责任人、截止时间、允许的操作、记录版本和处理经过。空负责人显示“未分派”，空位置显示“暂无位置”；没有独立处理时限时显示“未设置”，不能显示零分钟。

首次默认看全部待办，按严重程度从高到低、同级发生时间从早到晚排列，最后按事件编号稳定排序。每页 20 条，可切换 50 条。点摘要应用对应筛选；点击查询或重置回第一页，手动翻页保留筛选。选中事件时把编号记到页面地址，刷新后重新查详情。失去权限或记录不存在时说明原因，不保留旧人的详情。

操作约定：查看详情；确认新事件；为新建、已确认、已分派或处理中事件选择有权限的处理人；已确认或已分派事件由处理人开始处理，未分派时先分给自己；处理中填写措施和结果后完成；处理完成后按后台关闭规则关闭；误报必须填写理由，终结后只读。实际按钮由后台返回的允许操作决定，样板按同一规则模拟。

分派表单只列可分派的账号，确认后更新责任人。处理完成必须填写措施和结果；关闭或误报要显示对象、原因要求和影响。保存时按钮暂时不可重复点击；失败保留输入并允许重试；冲突时显示“此事件已被其他人更新”，重新读取后再决定操作。

本批人员侧栏展示姓名、部门、设备和最近读数，能返回事件。完整画像和地图尚未接入时显示“该入口尚未接入”，不伪造坐标、联系人或外部调用结果。

### G. F18 的状态检查步骤

1. 默认四条样本：数字与名单一致，打开甲的详情，人物、事件和测量时间匹配。
2. 筛选和翻页：搜索无结果时有重置入口，切换条件回第一页，返回列表保留条件；另用超过 20 条的独立分页样本验证真实翻页。
3. 确认与分派：确认甲后高危未确认减少；分派给当前用户后未分派减少、我的待办增加。刷新样板时的保留仅算模拟验证，Oracle 接入后必须重复验证。
4. 处理和关闭：开始处理、保存措施和结果、按条件关闭，待办数量更新；无法关闭时说明原因。误报有原因与记录，不能还提供处理按钮。
5. 无待办和无结果：前者用空数据源，后者用无匹配筛选，不能用一个含糊的“暂无数据”代替两种说明。
6. 首次加载、保存中、局部字段缺失、体征过旧分别检查。请求失败有旧数据时保留旧数据并显示时间；完全失败提供重试。
7. 只读角色看得见但没有修改权限；直接请求修改也被拒绝。记录被删或权限变化后不继续显示旧详情。
8. 双击只执行一次；两个窗口修改同一事件，后提交者收到冲突提示，不能覆盖先前结果。样板模拟与真实后台各验证一遍。

### H. 外观与尺寸怎么验收

与选定设计图比较时固定浏览器窗口、缩放、测试数据、测试时间、字体加载和动画状态。保存原图和实际网页截图，同一尺寸并排或叠加查看。不能将窗口尺寸不同的截图直接判为不一致。

每批都检查 1920×1080、1440×900、1280×800、1024×768、390×844 五档。窄屏允许筛选换行、次要表格列进入详情；手机详情全屏。表格内部需要时可横向滚动，但主页面不能因布局错误横向溢出。

通过标准：菜单、标题、筛选、主表和详情的位置及信息层次与选定图一致；主要文字、状态和按钮准确；无重叠、裁切、遮挡或点不到；长姓名、空值、长备注可读；详情内容能滚动且关闭和操作按钮可达；键盘能进入和退出侧栏，按钮有可见焦点。

字体渲染等小差异逐项记录，需要改变设计图中的明显布局时让用户看具体对比。不能只用一个“相似度百分比”决定通过，自动截图比较用于找差异，业务正确性另外检查。

当前前端已有 `pnpm build`，包含类型检查与生产构建。D2 开始前记录其基线，D3/D4 完成后运行并处理本次引入的问题。自动浏览器与结构检查目前需要实施时补齐，结果写入 acceptance.json，不能调用尚不存在的命令后声称通过。

### I. 验收记录必须写哪些内容

页面编号、图片版本、代码版本或当前差异摘要、工具仓库与提交版本、数据模式（演示/真实接口）、五档截图路径、每个操作检查结果、构建结果、仍有的差异、用户确认的视觉版本。失败项修复后重验相关部分，不能把未运行项填成通过。

外观通过后可以复制公共控件到下一批页面；真实接口和业务记录没通过前，只能称为“页面样板”。样板效果不好时先调整这一页的图或实现方式，不批量生成 31 页再返工。

## 4. F00：先做好通用菜单和控件

做好左侧菜单、顶部工具栏、全局人员搜索、连接状态、用户菜单，以及按钮、输入框、筛选、翻页、表格、图表、提示和详情侧栏。

菜单展开和收起都要能用。按钮要有选中、不可用、加载和错误等样式。通用控件集中维护，以后改一次就能在各页保持一致。

做完标准：工作台、实时监控和用户管理能共用这些控件，并在大屏和窄屏正常使用。

<details>
<summary>给设计工具的具体要求：通用样式、颜色和尺寸</summary>

```text
创建一个名为 HealthNext 的矿山职业健康与安全管理平台设计系统，并创建可复用的桌面应用外壳。产品用户是值班调度员、安全管理人员、职业健康管理人员和系统管理员。

设计目标：专业、克制、精密、适合长时间工作；重要的指挥和三维页面具有沉浸感。不要做营销网站、游戏大厅或夸张的赛博朋克界面。

使用深色工业主题：页面背景 #071018，一级表面 #0B151F，二级表面 #101D28，边界 #263946，主文字 #EAF2F5，次文字 #91A5AD。青色 #36C5D8 只表示实时信号和当前交互，绿色 #35C58A 表示正常，琥珀色 #E9AE52 表示关注，红色 #EF6262 只表示危急，蓝色 #5A8DEE 表示普通信息。禁止大面积紫色、蓝紫渐变、装饰光球、玻璃卡片堆叠和发光描边泛滥。

创建 Variables：颜色、4/8/12/16/24/32 间距、4/6/8 圆角、字号、边框和层级。中文字使用 PingFang SC，数字开启 tabular numbers。正文不得小于 12px，表格正文 13px，页面标题 20-24px。不要使用负字距。

创建 216px 展开和 64px 收起两种 Sidebar，56px Topbar。侧边栏一级导航包括：总览、安全指挥、健康监测、人员健康、预警处置、设备与数据、报告与智能、系统管理。Topbar 包含折叠按钮、面包屑、全局人员搜索、实时连接状态、通知入口和用户菜单。

用 Auto Layout 创建这些 Components 和 Variants：Sidebar、Topbar、PageHeader、FilterBar、Button、IconButton、Input、Select、DateRange、Tabs、StatusTag、MetricTile、DataTable、Pagination、EmptyState、ErrorState、StaleState、PersonSummary、VitalReading、ChartFrame、MapToolbar、MapLegend、Timeline、Drawer、Dialog、Skeleton。控件状态包括 Default、Hover、Active、Disabled、Loading、Error；数据状态包括 Normal、Attention、Warning、Critical、Offline、Stale、NoData。

卡片圆角不超过 6px，避免卡片嵌套。表格、筛选和分页强调效率；图表必须预留标题、单位、样本数、更新时间、图例和空态位置。使用专业线性图标，不使用表情符号或手绘图标。创建 Desktop 1440、Wide 1920 和 Narrow 1024 的应用外壳示例。
```

</details>

## 5. 31 个业务页面分别怎么做

### F01 登录页 `/login`

用来做什么：登录后进入系统。

怎么操作：输入账号密码，成功后进入工作台；失败时说明原因。

检查什么：账号密码错误、服务不可用和正在登录都有提示，手机上也能使用。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
在 HealthNext 设计系统中创建登录页，Desktop 1440×900 和 Mobile 390×844。使用真实矿井调度或井下作业照片作为全屏背景，降低对比度以保证文字清晰；左上显示 HealthNext 和“矿山职业健康与安全管理平台”。右侧设置约 380px 宽的登录区域，包含用户名、密码、显示密码、保持登录、登录按钮和后端服务状态。不要注册、社交登录和营销功能介绍。创建 Default、Submitting、WrongPassword、ServiceUnavailable 四种状态。登录成功的 Prototype 连接到工作台。
```

</details>

### F02 工作台 `/home`

用来做什么：知道今天先做什么。

怎么操作：查看我的待办、紧急事件、待复检人员和设备问题；点击数字进入对应名单。

检查什么：进入名单后保留刚才选择的条件，处理完返回时数字更新。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建 HealthNext 个人工作台 Desktop 1440×900。保持现有 Sidebar 和 Topbar。页面顶部显示当前班次、矿区、数据更新时间和手动刷新。首行用四个紧凑 MetricTile 展示“我的待办、今日高危、待复检准入、离线设备”，每项可点击下钻。主体左侧为按严重度和 SLA 排序的待办时间线，中间为今日健康态势和七日趋势，右侧为常用入口、实时连接与外部系统状态。人员姓名可点击，事件行可点击。设计 Default、NoTodo、CachedData、Loading 四个状态；不要做成领导驾驶舱，不要使用大面积装饰图。
```

</details>

### F03 健康监测总览 `/health-monitor/dashboard`

用来做什么：了解整体健康情况。

怎么操作：按日期、部门或工种看人数、有效数据和变化，再点进具体人员或事件。

检查什么：说明时间范围、覆盖人数和数据时间；总数来自后台统计。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建健康监测总览 Desktop 1440×900。顶部 FilterBar 包含日期范围、部门、工种和班次。第一行显示在册人数、当前在线、体征覆盖、当前异常，其中异常主值使用“异常人数 / 覆盖人数”。主体展示各指标覆盖与异常分布、部门风险排名、七日异常趋势、设备采集质量和重点人员表格。所有图表包含单位、样本数、更新时间和图例。让指标名称、部门柱形和人员姓名看起来可点击。创建完整数据、业务空数据、部分指标缺失、请求失败但保留缓存四种状态。
```

</details>

### F04 安全指挥中心 `/safety-command/index`

用来做什么：让值班人员集中处理重要风险。

怎么操作：查看紧急待办、未分派和超时事项，点事件看证据，点人员看详情，点地图看位置。

检查什么：严重事件醒目；地图、事件列表和数量保持一致。没有具体事件时不提供批量呼叫。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建 Wide 1920×1080 和 Desktop 1440×900 的安全指挥中心。使用沉浸式深色布局，但保留清楚的信息层级。顶部紧凑展示矿区、班次、日/周/月周期、实时连接、更新时间和全屏按钮。中央以井下态势小地图为最大区域；左侧展示预警总数、高危待办、待办总数、未分派、超时和重点风险人员；右侧是按严重度和 SLA 排序的实时事件流；底部展示部门风险、设备覆盖和处置时效趋势。事件行和地图事件标记连接到事件处置抽屉，人员连接到人员抽屉，区域连接到区域面板。红色仅用于危急，不提供脱离具体事件的批量呼叫。创建 Normal、NoIncident、RealtimeDisconnected、CachedData 四种状态。
```

</details>

### F05 井下态势地图 `/mine/map`

用来做什么：在井下巷道图上找到人和事。

怎么操作：开关图层、搜索定位、查看人员与事件、点击区域、回放历史轨迹，也能测距和缩放。

检查什么：标明坐标、比例尺、地图版本和位置时间；从其他页面进来时定位到对应对象。地图加载失败要提示。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建全屏井下态势 GIS 页面，Wide 1920×1080、Desktop 1440×900 和 Mobile 摘要版。中央地图占据主要空间，展示真实复杂的 CAD/Shapefile 巷道线网、井筒、水平、工作面和中文注记，使用工程坐标，不叠加城市道路。左侧可收起图层树包含巷道、注记、区域、人员、设备、预警和 SOS；右侧只显示当前选中人员、事件或区域的单个详情面板；右上是搜索、适应窗口、测距、图层、二维/轻量三维、放大缩小和全屏图标按钮；底部显示坐标、比例尺、地图版本和轨迹时间轴。标记状态包括正常人员、离线设备、普通预警和 SOS。设计搜索定位、选中事件、区域详情、轨迹回放、地图加载失败五个 Frame，所有浮层不得遮挡主要地图。
```

</details>

### F06 实时监控 `/health-monitor/real-time`

复核记录：Codex 独立验证默认名单不含离线人员、摘要与状态下钻人数一致、无在线空态、未来时间拒绝及 15/5 分钟包含边界，重新构建通过并检查 1440/390 截图。Grok 报告浏览器检查 74/74 通过，Codex 未独立重跑全部浏览器步骤。实时监控与值班名单保留独立菜单；真实接口与手表仍未接入。

用来做什么：查看当前在线人员的最新健康读数。

怎么操作：搜索人员，按部门、状态或指标筛选，翻页看名单；点姓名看详情，点关联事件去处理，点指标进专项分析。

检查什么：计划默认看 15 分钟内在线、5 分钟内的新读数，各项都有测量时间。正常、异常、过旧和无数据分别显示；相关事件不存在时不能把单条读数当事件。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建高密度实时健康监控页 Desktop 1440×900。页面顶部显示在线窗口 15 分钟、体征新鲜度 5 分钟、自动刷新倒计时、状态、指标、部门和人员搜索。摘要展示在线人数、当前异常、数据陈旧和无数据人数。主体使用服务端分页表格，列出人员、部门、设备、在线状态、心率、血压、血氧、体温或压力、每项采集时间、总体状态和预警原因。用稳定图标与文字区分 normal、warning、stale、no_data，不能只靠颜色。右侧允许展开紧凑事件栏。创建正常、仅看异常、缓存数据、无在线人员、加载状态。人员和异常原因需要明显可点击。
```

</details>

### F07 心率分析 `/health-monitor/heart-rate`

用来做什么：查看心率异常发生在哪些人、哪些部门。

怎么操作：选择日期、部门和工种，看变化曲线、部门统计和异常名单；点图表筛选，点人员看详情。

检查什么：区分当前异常与历史记录。部门图显示偏低、偏高记录条数；注明阈值、人数、极值和样本数。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建心率专项分析页。顶部包含日期、部门、工种和“当前异常”快捷筛选。摘要显示覆盖人数、异常人数、最低值、最高值和样本数。主体上方是带正常区间的心率时间趋势；下方左侧是各部门偏低与偏高记录数的分组柱形图，纵轴单位为“条”；右侧是服务端分页异常人员表，显示最近值、采集时间、异常方向和事件状态。图表注明阈值来源和数据更新时间。设计默认、当前异常下钻、部门下钻、无异常四个 Frame。
```

</details>

### F08 压力分析 `/health-monitor/pressure`

用来做什么：查看设备报告的压力指标和持续偏高的人。

怎么操作：筛选日期、部门、工种和压力等级，查看分布、变化及重点名单，进入个人历史。

检查什么：说明设备指标含义、采集时间和连续次数，不据此编造情绪标签或医学诊断。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建压力指数专项分析页，沿用 HealthNext 专项分析模板。顶部为日期、部门、工种和压力等级筛选。摘要显示覆盖人数、高压力人数、连续高压力人数和样本数。主体包括压力等级分布、时间趋势、部门高压力记录排名和重点人员分页表格。人员表显示最近压力等级、采集时间、连续次数和当前事件状态。明确这是设备压力指标，不生成情绪标签或医学诊断。创建 Default、DepartmentDrilldown、NoData、CachedData 状态。
```

</details>

### F09 血压分析 `/health-monitor/blood-pressure`

用来做什么：一起查看高压、低压两项血压读数。

怎么操作：选择时间和部门，看两项变化、偏高偏低记录及连续异常人员；点相关事件查看处理情况。

检查什么：读数带 mmHg 单位、测量时间和阈值范围，缺一项时明确标记，不能把单次异常说成疾病。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建血压专项分析页。摘要显示覆盖人数、异常人数、收缩压极值、舒张压极值和样本数。主趋势图同时展示收缩压和舒张压以及正常范围，不使用误导性的双轴。部门图分别统计偏高与偏低记录。异常人员表显示最近读数、mmHg 单位、测量时间、连续异常次数、事件状态和查看画像操作。设计默认、连续异常筛选、无异常、部分读数状态；单次异常不得写成疾病诊断。
```

</details>

### F10 血氧分析 `/health-monitor/blood-oxygen`

用来做什么：查看有效血氧读数和偏低人员。

怎么操作：看趋势、部门和异常名单；点无效数据统计，查看未佩戴或设备数据问题。

检查什么：有效值、低血氧、未佩戴和无效值分开。占位数字不能显示成正常，统计标明有效覆盖人数。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建血氧专项分析页。摘要显示有效覆盖人数、低血氧人数、最低有效值、无效或未佩戴读数。主体包含有效血氧趋势、阈值分布、部门低血氧记录和异常人员分页表格。表格显示人员、设备、最近有效值、采集时间、无效原因和事件状态。无效读数使用 NoData 语义，不显示为正常；明确百分比单位。创建正常、低血氧下钻、无效数据下钻、完全无有效样本四个 Frame。
```

</details>

### F11 睡眠分析 `/health-monitor/sleep`

用来做什么：查看昨夜睡眠资料和上传情况。

怎么操作：选择日期、部门和班次，看时长分布、深浅睡结构、七日变化和人员明细。

检查什么：注明数据日期与覆盖人数；部分人没上传和完全没数据都要说清，不编造评分。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建睡眠分析页。顶部选择日期、部门和班次。摘要显示昨夜上传覆盖、平均睡眠时长、达标人数和需关注人数。主体展示睡眠时长分布、深睡浅睡结构、七日趋势、部门上传情况和人员明细。所有统计注明覆盖人数和数据日期。创建有数据、部分上传、没有收到睡眠数据、缓存数据四个状态；不要生成虚假的睡眠评分或医学判断。
```

</details>

### F12 趋势预警 `/health-monitor/trend-warning`

用来做什么：了解一段时间的变化为什么值得关注。

怎么操作：选择指标、时间窗口和人员风险，看原始曲线、样本和规则说明，再进入有关事件。

检查什么：区分趋势风险和当前读数超限；数据不足或规则停用时说明原因，不作确定性医学判断。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建趋势预警页，明确区分趋势风险和当前阈值越界。顶部提供指标、趋势窗口、部门、风险等级和状态筛选。左侧为趋势风险队列，显示人员、方向、连续变化天数、证据强度、当前是否越界和事件状态；右侧展示选中人员的原始时间序列、阈值线、趋势区间、样本数和规则说明；底部显示生成与处置审计。创建选中风险、无趋势风险、样本不足、规则停用状态。文案保持概率和证据语气，不制造确定性医学判断。
```

</details>

### F13 风险预警 `/health-monitor/risk-warning`

用来做什么：统一查询不同原因产生的风险事件。

怎么操作：按来源、严重度、状态、部门和时间筛选，点事件处理，点人员查看。

检查什么：体征超限、设备主动报警、趋势风险分别标明；严重度单独显示，SOS 只能属于设备主动求救。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建风险预警查询页。将 HEALTH_THRESHOLD、DEVICE_ALARM、TREND_WARNING 三种来源用清楚的中文标签区分，严重度作为独立维度。顶部筛选来源、事件代码、严重度、生命周期状态、部门和时间。摘要按来源与状态聚合，主体为可排序服务端分页表格，显示事件、人员、位置、发生时间、来源、严重度、责任人、SLA 和状态。事件行连接事件抽屉，人员连接人员抽屉。SOS 只允许出现在设备主动报警。创建默认、高危筛选、无结果和加载失败状态。
```

</details>

### F14 职工档案 `/health-monitor/employee-archive`

- [x] 本批 mock 样板已完成：查询、新增、编辑、人员详情、事件入口和设备绑定一致性。批量导入、地图及完整健康页联动未在本批验收，真实业务接口未接入。

用来做什么：登记和查找职工。

怎么操作：按姓名、工号、电话或手表编号搜索；新增、编辑、导入，查看健康页或地图。

检查什么：展示部门、工种、在职状态和设备绑定。导入先检查再提交；联系方式按权限隐藏部分内容。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建职工档案列表页。顶部提供姓名、工号、手机号、IMEI 搜索，以及部门、工种、在职状态、设备绑定状态筛选。主体使用高密度表格，显示人员身份、部门、工种、脱敏联系方式、设备绑定、最近在线、健康数据更新时间和当前风险。行操作包括查看画像、地图定位和编辑。设计新增/编辑人员抽屉、批量导入入口、导入校验结果、空档案和无权限状态。不要使用人物卡片瀑布流。
```

</details>

### F15 职工健康画像 `/health-monitor/employee-profile`

- [x] 本批 mock 样板已收尾：历史日期查询、可变台账同步、事件状态映射、刷新缓存隔离、深色控件和响应式布局。

复核证据：Codex 独立验证 7/14/30 天及自定义历史点数、人员改名与设备解绑同步、0% 电量显示，重新构建通过，并检查 1440/390 页面及手机日期弹层截图。Grok 报告浏览器 52/52 通过，Codex 未独立重跑全部浏览器检查。C01 本批样本验证至处理完成，关闭/误报未实测；真实后台、联系与设备控制仍未接入。

用来做什么：集中查看一个人的健康资料。

怎么操作：切换人员，看身份、当前读数、七日变化、今日活动和预警；选择日期查历史，也能联系或进地图、三维人体。

检查什么：每个指标单独显示采集时间。历史查询说明样本数和汇总方式；无设备、无历史、读数过旧和刷新失败有明确提示。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建职工健康画像 Desktop 1440×900。顶部提供人员搜索、上一人/下一人、手动刷新、自动刷新倒计时。身份摘要显示姓名、工号、部门、工种、设备和当前位置。主体左侧展示当前体征，每个指标独立显示数值、单位、采集时间、新鲜度与状态；中部展示七日趋势和今日活动；右侧展示设备状态、联系处置、地图定位和近 30 日预警轨迹。历史区域提供日期范围和查询按钮，并显示样本数与聚合粒度。提供醒目的“沉浸人体”入口。创建正常、人员无设备、体征陈旧、无历史、刷新失败但保留数据五个 Frame。
```

</details>

### F16 沉浸人体 `/health-monitor/body-360-immersive`

用来做什么：用可旋转的人体查看真实体征。

怎么操作：选择人员，旋转、缩放人体，点指标标记看数值、阈值、趋势和事件；可关闭自动旋转或减少动画。

检查什么：数据来源真实；模型加载失败、电脑不支持或性能不足时仍能看读数。不虚构器官病变或心电图。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
基于现有 Body360ImmersivePage.vue 完善 HealthNext 沉浸人体页面，Wide 1920×1080 和 Desktop 1440×900。中央是可拖拽旋转、缩放的真实全身 GLB 线框人体，使用克制的青绿色扫描材质、局部扫描线和少量粒子，场景全幅展开，不放在装饰卡片中。人体热点对应心率、血氧、体温、压力和血压，热点颜色来自真实状态。左上提供人员搜索、身份和返回健康画像；右侧紧凑展示当前选中指标的数值、单位、采集时间、阈值、最近趋势和相关事件；底部使用图标按钮提供自动旋转、重置视角、减少动态和全屏。设计正常人体、选中异常热点、模型加载、WebGL 不可用、低性能降级、人员无数据六个 Frame。不要绘制虚构器官热区或伪造 ECG。
```

</details>

### F17 入井健康准入 `/health-monitor/mine-entry`

用来做什么：完成入井前的健康检查和人工决定。

怎么操作：选日期、班次和人员，看有效体征与规则依据，安排复检、确认准入或禁止入井，再处理下一人。

检查什么：无效和过旧读数不能当合格证据；人工改变建议需写原因并确认，保存复检和决定经过，完成后刷新人数。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建班前入井健康准入页。顶部选择日期、班次、部门和准入状态，摘要显示应检、已检、准入、待复检、禁止入井。主体左侧是人员检查队列与进度，右侧是选中人员的身份、设备、最新有效体征、每项采集时间、规则命中、复检记录和建议结论。固定操作区包含确认准入、安排复检、禁止入井；人工覆盖建议时必须填写原因。设计待检查、建议准入、需要复检、禁止入井、体征无效、已完成六个状态，并建立连续处理下一人的 Prototype。
```

</details>

### F18 待办事件 `/alert-management/notifications`

- [x] 本批 mock 样板已完成：筛选、摘要、分页、C01 处置流程、动作约束与紧凑时间展示。最后一轮时间展示依据 Grok 报告，真实接口未接入。

用来做什么：集中处理还没办完的事件。

怎么操作：看我的待办、高危未确认、未分派和超时事项，按条件筛选；快速确认、分派，或打开详情处理。

检查什么：确认、分派、处理中分别显示。处理后保留筛选并更新列表；没有独立阅读记录前不做“已读”。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建值班人员待办事件页。顶部筛选严重度、来源、分派状态、SLA、部门和时间；摘要显示我的待办、高危未确认、未分派和已超时。主体为高密度分页表格，状态严格区分新建、已确认、已分派、处理中。行内只保留查看、确认和分派三项高频动作，其他操作进入事件处置抽屉。显示人员、位置、证据摘要、责任人、剩余 SLA 和更新时间。创建默认、我的待办、高危超时、无待办和处理后刷新状态；不要设计“已读”。
```

</details>

### F19 设备紧急事件 `/alert-management/sos`

用来做什么：处理手表主动发来的报警。

怎么操作：按求救、跌倒、拆卸、低电等类型查看事件，查人员、设备、位置和处理经过。

检查什么：真实 SOS 与普通设备报警分开，严重程度另外说明；不混入体征超限事件，未绑定设备也要明确显示。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建设备紧急事件页。顶部展示未处理设备报警、真正 SOS、跌倒事件和离线关联设备数量，并提供事件代码、严重度、状态、部门和时间筛选。主体左侧为按严重度与时间排序的事件列表，右侧展示选中事件的人员、设备、井下位置、最近通讯、证据和处置时间线。提供打开事件处置、人员详情、设备详情和地图定位。SOS、跌倒、拆卸、低电使用不同代码和图标，严重度单独显示。创建真实 SOS、普通设备报警、未绑定设备、无事件状态；不得混入健康阈值事件。
```

</details>

### F20 风险规则配置 `/alert-management/config`

用来做什么：设置什么时候报警、怎样通知和多久处理。

怎么操作：选择规则，编辑草稿，检查内容，比较改动，确认发布；查看旧版本并按权限恢复。

检查什么：草稿不生效，发布后才生效。健康规则有数值与持续时间，设备报警没有健康数值阈值；未接入策略不能假装启用。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建风险规则配置页。左侧按健康阈值、设备报警、趋势规则分组并显示启用状态；右侧使用 Basic、Conditions、SeverityAndSLA、Notification、VersionHistory 页签。健康规则包含指标、适用人群、上下限、持续时间、严重度和 SLA；设备报警没有数值阈值，只设置启用、严重度、SLA 与通知；未接入策略明确标记“未接入”并禁用。顶部提供保存草稿、校验和发布，发布确认展示字段变化与影响范围。设计编辑草稿、校验错误、发布确认、已发布和历史版本状态。
```

</details>

### F21 处置记录 `/alert-management/records`

用来做什么：查已经处理的事和完整经过。

怎么操作：按事件、人员、处理人、时间和结果筛选，点开只读详情，导出当前查询结果。

检查什么：保存发生、确认、分派、处理和关闭时间，说明是否超时，展示备注、附件和外部操作真实结果。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建处置记录审计页。顶部筛选事件来源、代码、严重度、处理结论、处理人、部门和时间。主体表格显示事件、人员、发生时间、确认时间、分派时间、处理时间、关闭时间、SLA 是否超时、处理人和结论。详情使用只读事件抽屉，时间线展示每次状态变化、备注、附件和外部动作真实结果。设计默认、误报筛选、超时筛选、导出中、无记录状态；外部系统未配置时显示 NOT_CONFIGURED。
```

</details>

### F22 设备管理 `/admin/device-list`

- [x] 本批 mock 样板已完成：筛选、摘要、分页、新增、编辑、导入校验、绑定、解绑、停用及逐项批量结果。跨页绑定冲突、失败不写入和稳定设备 ID 已独立验证，1440 时间截断已修复；真实接口未接入。

用来做什么：管理手表资料和佩戴关系。

怎么操作：搜索设备，登记或批量导入，绑定、解绑、停用；从设备查看人员、控制入口和通信记录。

检查什么：显示型号、在线、电量、最近通信、最后数据时间及绑定历史；导入和批量操作说明哪些成功、哪些失败。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建设备管理页。顶部支持 IMEI、型号、在线状态、绑定状态、电量和部门筛选；摘要展示设备总数、在线、离线、低电和未绑定。主体表格显示设备、绑定人员、最近在线、权威电量、固件、状态和最后数据时间。设备名称和 IMEI 可打开设备详情抽屉，人员可打开人员抽屉。设计新增设备、批量导入校验、绑定人员、解除绑定、停用确认、空设备六个状态。操作结果必须包含成功与失败明细。
```

</details>

### F23 手表控制 `/health-monitor/watch-control`

用来做什么：对指定手表发起允许的测量或控制。

怎么操作：选设备和命令，确认后查看发送、设备确认、测量返回或超时的过程；收到读数后可看人员健康页。

检查什么：命令已发出、设备已收到、结果已回来是三件事。离线、未绑定或任务冲突时禁用不允许的操作，并说明原因。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建手表控制台。左侧为设备搜索和在线设备列表，中间展示选中设备、绑定人员、电量、最近通讯、当前任务，右侧为允许的控制命令。立即测量按心率、血压、血氧、体温分成独立图标按钮。底部时间线明确区分“命令已下发、设备已确认、测量数据已回传、超时或失败”，AP 命令确认不等于得到测量结果。离线、未绑定和任务执行中时禁用不允许操作。设计命令确认弹窗、执行中、成功回传、确认但未回传、离线状态。
```

</details>

### F24 手表原始报文 `/health-monitor/watch-raw`

用来做什么：帮助维护人员查手表通信故障。

怎么操作：按设备、消息类型、收发方向、时间和错误筛选；暂停滚动，点开一条消息看内容、解释结果和失败原因。

检查什么：敏感内容按规则隐藏；暂停或刷新时保留选择，消息太多时使用翻页或只加载需要的部分。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建面向运维人员的手表原始报文页。顶部筛选 IMEI、协议代码、收发方向、解析状态和时间范围，并有暂停实时滚动开关。主体使用服务端分页或虚拟滚动日志表，显示接收时间、设备、协议类型、方向、原始报文摘要、解析结果和耗时。详情抽屉用等宽字体展示脱敏原始文本、结构化字段、处理步骤和错误原因，并提供复制。创建实时流、暂停、解析失败、无报文和连接中断状态。
```

</details>

### F25 报告中心 `/health-monitor/report-center`

用来做什么：生成、查看和下载报告。

怎么操作：选择报告对象、类型和日期，提交后查看排队、生成和完成状态；预览下载，失败时重试，也能取消。

检查什么：报告注明覆盖范围、样本数和数据截止时间，人员报告能进入对应人员；AI 内容明确是辅助分析。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建报告中心。顶部包含报告类型、对象、时间范围、生成状态和搜索，并提供新建报告按钮。主体左侧为报告任务表格，显示名称、覆盖范围、样本数、生成方式、创建人、状态和更新时间；右侧为选中报告预览，展示摘要、数据截止时间、风险提示和目录。提供生成、取消、重试、预览和下载。设计新建报告抽屉、排队中、生成中、成功、失败、无报告状态。AI 报告明确标记“辅助分析，不作为医学诊断”。
```

</details>

### F26 AI 健康助手 `/ai-chat/index`

用来做什么：帮助查询、解释已有数据并起草报告。

怎么操作：选择任务或提问，查看引用的数据、人员和事件，点击查看依据，把草稿转到报告中心。

检查什么：显示数据范围、日期、样本与来源；越权查询要拒绝，失败要说明，健康解释不替代医学诊断。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建受控的职业健康 AI 助手。左侧为会话历史和新建会话，中间为对话区，右侧为本次分析的数据范围、日期范围、样本数、引用来源和权限说明。空会话提供查询全矿概况、分析部门趋势、解释人员报告、生成报告草稿四种任务入口。回答内的人员、事件和数据引用可点击下钻；生成报告草稿连接到报告中心。设计空会话、分析中、有引用回答、无权限数据、分析失败状态。避免普通聊天机器人营销界面，所有健康结论标记为辅助分析。
```

</details>

### F27 用户管理 `/admin/user-list`

用来做什么：管理登录平台的账号。

怎么操作：新增、编辑、分配角色和可查看部门，停用账号、重置密码、查看操作记录。

检查什么：重要操作确认后执行，保存后刷新当前行并保留筛选；账号权限在服务器检查。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建后台用户管理页。顶部筛选账号、姓名、角色、部门和状态；主体表格显示账号、姓名、角色、部门、状态、最近登录和创建时间。提供新增、编辑、重置密码、启停和查看审计。新增/编辑使用右侧抽屉，分为基本信息、角色和数据范围；危险操作使用确认弹窗。设计默认、新增用户、编辑用户、停用确认、无权限、空列表状态。页面保持朴素高效，不添加无关图表。
```

</details>

### F28 角色管理 `/admin/role`

用来做什么：规定一类账号能看什么、做什么。

怎么操作：选择角色，设置菜单、按钮和部门范围；保存前看新增或移除的权限，查看关联用户。

检查什么：区分全部、本部门、下级部门和自定义范围；有用户关联时不能直接删除角色，未保存改动有提示。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建角色与权限管理页。左侧角色列表显示名称、状态和成员数；右侧包含基本信息、菜单权限、按钮权限、数据范围和变更记录页签。权限树支持搜索、展开、半选；数据范围明确区分全部、本部门、本部门及下级、自定义部门。保存前展示新增和移除权限摘要。设计新建角色、编辑权限、未保存变更、保存确认、存在关联用户无法删除状态。
```

</details>

### F29 部门管理 `/admin/department`

用来做什么：管理部门及上下级关系。

怎么操作：搜索部门树，查看负责人和人数，新增同级或下级，移动、编辑、停用；点人数进入对应名单。

检查什么：停用和移动前说明影响，大型部门树仍能使用，查看人员时自动带入该部门条件。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建部门组织管理页。左侧是可搜索、展开和收起的部门树，右侧显示当前部门基本信息、负责人、人员数量、设备数量、下级部门和状态。提供新增同级、新增下级、移动、编辑和停用。人数与设备数可下钻到相应列表。设计根部门、普通部门、编辑抽屉、移动部门、停用影响确认、空组织状态。布局应支持大型组织树并避免多层卡片。
```

</details>

### F30 工种管理 `/admin/job-type`

用来做什么：管理工种及适用的健康规则。

怎么操作：按名称、风险分类和状态查工种，新增编辑，点人数查人员，点规则查看配置。

检查什么：显示编码、关联人数和更新时间；停用前说明对规则和准入的影响。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建工种管理页。顶部筛选名称、风险分类和状态；主体表格显示工种名称、编码、风险标签、关联人数、适用健康规则、更新时间和状态。新增/编辑抽屉包含名称、编码、风险分类、说明和规则关联。人数可下钻职工档案，规则可进入风险规则配置。设计默认、新增、编辑、停用影响确认、无工种状态。
```

</details>

### F31 系统设置 `/admin/settings`

用来做什么：集中修改系统设置，查看外部服务状态。

怎么操作：选择安全、实时监控、地图、通知等分类，修改并保存；需要时测试外部服务连接。

检查什么：区分未配置、连接正常和连接失败；敏感值只显示已设置，测试连接不自动保存，未保存和错误都有提示。

<details>
<summary>设计人员参考：该页的详细工具提示词</summary>

```text
创建系统设置页。左侧按基础信息、安全策略、实时监控、地图与资源、外部系统、通知和审计分组；右侧显示对应表单。敏感配置只显示“已设置”状态，不显示明文；外部系统状态区分未配置、连接正常、连接失败，并提供测试连接。顶部显示未保存变化，底部提供取消和保存。设计普通设置、未保存、校验错误、保存确认、测试连接中、无权限状态。
```

</details>

## 6. 五种公共详情和提示

### C01 事件详情与处理

- [x] 本批 mock 样板已完成，F18/F14/F22/F06 已复用，包含状态动作、证据、时间线和实时名单。预警记录及其他页面全面接入、正式权限与持久化仍待完成。

所有页面打开同一种事件详情。查看人员、位置、异常依据、严重程度、发生时间、负责人、处理时限、备注、附件和完整经过。

根据状态提供确认、分派、开始处理、处理完成、误报和关闭。查历史记录时只能查看。身体恢复情况和工作人员处理情况分别说明；关闭条件按后台计划第 16 节实施，不能仅因读数正常就当作事情已处理。

桌面在右侧打开，手机全屏。查看关联人员时在同一区域切换内容，并可返回这条事件。

### C02 人员快速详情

显示身份、部门、工种、按权限隐藏部分内容的联系方式、设备、位置、最新读数及时间，还有未处理事件。可以进入完整个人健康页、人体页、地图和联系功能。

没有设备、设备离线、无读数和读数过旧分别提示。只有设备已绑定且在线、功能已接入时才允许发送消息或语音。管理端不能伪造“发送 SOS”。联系模式集中显示联系所需内容。

### C03 设备详情

- [x] 本批 mock 样板已完成，包含固件、网络、维护、绑定历史、近期错误、人员详情及响应式抽屉。控制与原始报文仅提示尚未接入，不能标为设备业务完成。

显示设备编号、型号、在线和电量、佩戴人员、最近通信、最后读数、固件、网络、维护记录、绑定历史和错误。提供绑定解绑、控制、通信记录和停用入口。

在线未绑定、离线、低电、已停用和加载中的情况都要设计。操作是否允许由实际状态和权限决定。

### C04 井下区域详情

点地图区域时，显示名称、类型、地图版本、人数、在线设备、未处理事件、环境信息和负责人。可以查看人员、事件、聚焦区域或按已接入能力联系负责人。

正常、需要关注、危急、无人员和数据过旧都要有提示；负责人或环境数据未接入时如实显示。面板不能遮住主要地图。

### C05 通用提示

统一提供加载中、没有数据、筛选无结果、部分数据、刷新失败但有旧数据、完全失败、无权限、数据过旧、页面不存在和系统维护提示。

说明原因、最后数据时间和能做的下一步，提示放在正常布局里，不用大幅空白遮住原本有用的信息。

<details>
<summary>设计人员参考：五种公共详情和提示的具体样式</summary>

## 7. 必须能点击演示的五个过程

### A：从发现风险到处理结束

登录 → 工作台点高危事件 → 查看待办 → 打开事件 → 查人员和位置 → 返回事件 → 确认并按需分派 → 记录处理结果 → 按规则关闭 → 返回列表看到数量更新。

### B：从人员到测量依据

搜索人员 → 快速详情 → 个人健康页 → 查看异常心率和专项记录 → 返回 → 打开三维人体并点心率标记 → 查看关联事件 → 返回个人健康页。

### C：在井下快速定位

指挥中心点地图事件 → 看事件详情 → 进入完整地图并定位 → 查看附近人员 → 打开人员详情 → 按设备和服务条件联系处理。

### D：入井前检查

工作台点待复检人员 → 进入入井检查 → 看体征和规则依据 → 安排复检 → 收到新读数 → 人工确认准入 → 保存结果并进入下一人。

### E：排查设备问题

工作台点离线设备 → 设备列表自动筛选 → 设备详情 → 查看收发消息 → 返回 → 手表控制 → 因设备离线而禁用不允许的操作，并说明原因。

浏览器样板或可选 Figma 设计稿可以使用明确标记的演示数据检查点击顺序。正式功能验收时，必须再用实际接口和设备数据验证。

## 8. 每页怎样算设计做完

- 一眼能看出页面用途和最常用操作。
- 字段、单位、统计时间、人数和更新方式说清楚。
- 人员、设备、事件和区域入口一致，点进去有内容和返回入口。
- 跳转后筛选条件可见，返回后列表状态得到保留。
- 加载、空数据、筛选无结果和失败都已画好；实时页补过旧和断连，操作页补权限和不可用状态。
- 1440、1920 和 1024 宽度都检查过，手机需要的操作也能使用。
- 图表有单位、图例、样本数和无数据提示；红色用于危急或破坏性操作。
- SOS、测量异常和趋势风险没有混淆；人体和地图能打开真实人员与事件入口。
- 浏览器样板能点通相关过程，公共控件统一复用；采用 Figma 时可另行归档设计稿。

选定图并记录文字、动作和状态，说明设计材料已齐；浏览器操作、异常情况和多屏检查通过后，记为页面可操作；接入真实后台并验证后，才算功能完成。Figma 是否归档不影响这套标准。

## 9. 设计完成标准

页面完成前必须有：选定图、准确文案、字段和状态说明、可点击样板、五档尺寸检查、构建结果和未完成项。真实接口接通后，再按后台计划验证权限、分页、状态保存和错误处理。

具体页面要求见本文件各 F01–F31 小节；公共详情见 C01–C05。工具提示词只是视觉参考，不能替代业务规则。
