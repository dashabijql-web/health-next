只改 HealthNext 360° 人体页的布局，不要加新的大板块，不要改井下地图，不要改后端，不要重写 Three.js 的 initScene / loadModel / animate / dispose。

仓库：/Users/jiangqianli/Documents/code/HealthNext
页面：http://localhost:9531/health-monitor/body-360  （admin / admin123）
文件主要是：
- web/src/modules/human-body/Body360Page.vue
- 如必须，可动 web/src/modules/human-body/BodyHologram.vue 的外层尺寸，不要动场景逻辑
- 不要为这件事新建一堆组件

# 问题
左栏（身份卡 + 遥测 + 发消息/语音/电话）比右边「3D 人体 + 走势」矮，走势条只铺在人体下面，左下角空一大块。不要用重复的当前心率/血氧去填这个洞。

# 按这个结构改
1. 主区改成两列网格，左栏和右栏同一行等高（stretch）。
2. 左栏内部用纵向 flex：身份和遥测在上，中间可以留空白，**「联系此人」钉在左栏最底部**，下沿和右列走势条下沿对齐。
3. **走势条拉满整行宽度**，从左栏左边一直铺到人体右缘。也就是：上面仍是「左身份 | 右 3D」，下面走势单独一行通栏。五条小曲线变宽，不要挤成细条。
4. 3D 人体主舞台高度不能被走势条吃掉；走势条保持大约 120～160px。
5. 发消息 / 语音提醒 / 打电话·未配置 的逻辑不要改：未在线禁用消息和语音；电话保持未配置；发送只能提示未真正下发。

# 不要做
- 不要在左下角再堆一块当前体征数字
- 不要加步数、事件时间线、热区、ECG、疲劳分、AI 建议
- 不要把选人改回半屏长列表
- 不要为填空而加装饰英文卡

# 窄屏
左栏在上、人体在中、走势在下，通栏走势仍全宽。联系按钮保持可点，不要和走势重叠。

pnpm build 必须通过。做完说明：左下空是怎么消掉的、走势是不是已经通栏、人体高度有没有被压没。
