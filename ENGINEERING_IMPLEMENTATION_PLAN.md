# HealthNext 后台实施计划（通俗版）

前端进度：F18/C01、F14、F22/C03、F06 实时监控和 F15 职工健康画像的本批 mock 样板已完成，具体范围与验收证据见 [总体计划的已完成与待办](./PRODUCT_DESIGN_PLAN.md#已完成与待办)。绑定校验和实时名单目前只在前端 mock 数据层生效，不代表 E 阶段或 D4 已完成；Oracle、真实设备和正式业务接口继续按本文待办实施。

人体展示默认使用「沉浸人体」：`/health-monitor/body-360-immersive`，实现为 `web/src/modules/human-body/Body360ImmersivePage.vue`。原「360° 人体」`/health-monitor/body-360` 保留为非默认页面，不再作为新开发和视觉验收基准；人员档案、事件详情和健康画像的人体入口应进入沉浸人体，并携带当前人员上下文。

这份计划讲页面背后的工作：怎么接手表、怎么保存数据、怎么报警、怎么记录处理结果，以及怎样保证系统能正常运行。页面外观和点击方式见 [页面设计计划](./FIGMA_PAGE_DESIGN_PLAN.md)，整体顺序见 [总体开发计划](./PRODUCT_DESIGN_PLAN.md)。

数据库已经确定用 **Oracle**。文中的“要做”和“做完标准”是待完成的工作，不是已经通过的结果。

## 1. 第一批要做到什么

能登记一个人，给他绑定一块手表，收到有效心率和血氧。持续异常时出现一条事件，管理员可以查看、确认、分派和记录处理结果。刷新页面后，数据、状态和处理经过仍然保留。

做到这一点，才算做出了第一条能用的业务流程。前面的已有列表页改造和 Grok Build mock 样板按总体计划 D0–D3 执行，真实数据接入按 D4 执行。样板没有接数据库时，只记录页面验证结果，不能记为业务已经完成。

## 2. 已有基础和还要做的工作

根据原计划编写时的检查，HealthNext 已有程序框架、登录退出、基本页面跳转，以及地图和三维人体的展示验证。部分页面还使用演示数据。

正式数据库、人员设备管理、手表采集、报警处理、真实查询和上线保障，当时还没有完成。本次是改写计划，没有重新检查代码，因此这里不能当成最新完成率。

接下来每做完一批，都用实际运行、测试结果和演示证明完成。

## 3. 已经确定的做法

- 用 HealthNext 做独立新系统，业务数据统一保存在 Oracle。
- 旧系统提供通信规则、业务经验和测试样本。需要旧记录时，先导出整理，再导入。
- 服务器程序先集中运行，内部按账号、人员、设备、数据、事件、地图和报告分工。
- 历史读数、每项指标的最新读数、需要处理的异常事件，分别保存。
- 页面上的结果以服务器保存的记录为准。收到更新通知后，再向服务器读取最新内容。
- 先使用必要的基础工具；实际出现速度、积压或恢复需求时，再引入缓存和排队工具。
- 外部系统没配置、设备没回结果时，如实显示，不能假装执行成功。

## 4. 系统需要保存哪些东西

### 账号与组织

保存账号、角色、权限、部门和工种。记录重要操作的执行人、时间和内容。谁能看什么、能操作什么，由服务器检查。

### 人员与手表

保存人员档案、设备资料、在线状态、电量，以及绑定和解绑历史。必须知道某个时间段内手表给谁佩戴，不能只记现在绑定的人。

设备控制还要记录请求、发送、设备确认、测量结果、失败和超时。

### 健康数据

保存手表原始消息、解释后的测量记录、各指标最新有效值和每日汇总。分别记录设备测量、服务器接收和保存时间。

例如心率是刚测的，血氧是十分钟前测的，页面就分别显示这两个时间。心率更新不能让血氧也变成“刚刚更新”。

### 风险与处理记录

保存当时使用的规则及版本、事件、处理人、处理动作、通知发送结果和待发送通知。每件事使用自己的稳定编号，避免认错历史事件。

### 地图与位置

保存地图原文件、发布版本、图层、巷道和区域，以及人员最新位置和历史轨迹。每个位置都能查到来源、采集时间、采用的坐标和地图版本。

## 5. 数据库具体怎么准备

先核实要部署的 Oracle 版本、地址、连接方式和可用功能，准备开发与测试使用的独立空间。环境允许时可以用容器；也可以连接专门的测试实例。

数据库建表和以后升级，都保存成带版本的脚本，方便在新环境重复执行。不能等用户点开业务页面时临时建表。

历史数据按测量时间建立查询索引并安排归档。是否分区、如何分区，在核实 Oracle 环境和数据量后决定，不耽误第一条业务流程。

地图计划使用 Oracle 的空间数据功能，正式使用前验证坐标、空间查询和索引。连接工具和数据库升级工具也要针对目标版本验证。

## 6. 手表每发来一条数据，系统做什么

1. 认出是哪块手表，并保存可追查的原始消息。
2. 查明这次测量属于哪个人员。
3. 读出心率、血氧等字段，检查消息是否重复、读数是否有效。
4. 保存有效测量记录，更新该人员对应指标的最新值。
5. 按已发布的规则判断是否产生异常或进入恢复观察。
6. 保存事件变化和待发送通知，再让相关页面更新。

第一条流程先做心率和血氧，完整接入范围还包括设备登录、定期报平安、血压、体温和设备主动报警。位置只有在现场来源可靠时才启用。

必须守住这些要求：

- 重复发送同一条消息，不能反复新增读数和事件。
- 未佩戴、无效和占位值要保留原因，不能当成正常体征。
- 已验证的 APHP 未佩戴占位血氧不能当有效值；错误的零电量不能覆盖已有有效电量。
- 过晚到达的数据如何归人、是否更新最新值，要按时间和绑定历史判断。
- 保存数据暂时失败时，要能看见失败或积压，按确定的恢复办法处理。

缺少设备测量时间、重复、乱序、重新绑定和断报按第 16.4 节实施，并在 E3 用实际协议样本补成检查例子。原有“设备、消息类型、测量时间、消息内容”组合只能作为识别重复的起点，不能当成已经解决了所有情况。

## 7. 什么时候报警，什么时候算处理完

### 报警规则需要写什么

写清监测哪项指标、适用于哪些人、上下限、异常持续多久、恢复正常多久、严重程度、处理时限、是否启用和规则版本。

数值在边界附近反复波动时，要有避免频繁报警和恢复的办法。具体参数需在规则发布前确定，这份计划不随意给出医学阈值。

同一次连续异常保留一个活动事件。短暂正常先观察，达到恢复条件后才标记稳定恢复。

### 两种状态分别保存

一是体征情况：正在异常、恢复观察中、已经稳定恢复或因数据不足无法判断。

二是工作人员处理情况：新事件、已经确认、已分派、处理中、处理完成、已经关闭，或经核实标记误报。

“体征恢复”不会自动抹掉事件和处理记录。“确认”表示工作人员已经确认这件事；“处理完成”需要有结果；“关闭”按约定条件结束处置。未建立独立的通知阅读记录前，不提供虚假的“已读”。

第一版动作和关闭规则按第 16 节执行，页面样板也模拟同一组规则。健康阈值、异常持续时间和恢复持续时间来自经确认并发布的规则配置；没有有效配置时显示未配置，不由生图或页面代码给出临床数值。

### 区分为什么产生事件

- 体征超限：心率、血氧等读数触发健康规则。
- 设备主动报警：手表主动上报求救、跌倒等信息。
- 趋势风险：一段时间内的变化触发趋势规则。

严重程度另外记录。SOS 只指设备主动求救，高危读数不能改名叫 SOS。设备报警不配置健康数值阈值，按已接入能力设置启停、严重度、处理时限和通知策略；未接入策略明确禁用。

## 8. 页面怎么拿到正确、及时的数据

筛选、翻页和统计由服务器完成。页面不能用已经加载的几十条记录推算全体人数。一个人有多项异常时，异常人数仍按一个人计算，事件数按事件统计。

返回的数据要说明单位、时间范围、采集时间、最近刷新时间和是否过旧。查历史趋势时，说明是原始记录还是按时间汇总，并提供真实样本数。

事件变化后通知页面重新查询；通知失败可以重试。断线后重新连接，要能补上期间的变化并避免重复处理。

多人同时操作同一件事，要检查是否已被别人修改。重复点击不能重复执行。输入不合要求、没有权限和服务失败时，要给出明确原因，并留下便于排查的错误编号。

通知能补多久、积压多少算异常、超过补发范围如何重新同步，在 E5 确定并验收。

## 9. 地图和其他功能怎么接上

地图原文件先检查坐标、单位和图层，转换后由人工核对巷道、井筒、工作面和文字，再发布一个可追查的版本。地图按可见范围和缩放程度加载，人员和事件作为单独图层显示。

任意一个人员点都要能找到设备、人员、来源和采集时间。没收到位置时，显示没有位置；旧位置显示时间，不能编造新坐标。

后续还要做入井规则与人工复核、报告生成和下载、设备控制与回执、原始消息查询、AI 数据引用、外部通知。每项操作都要有权限、结果和记录。

## 10. 旧数据怎么搬过来

先确定保留哪些数据。从旧系统导出文件，隐藏或控制敏感信息，整理字段和编号，检查重复、缺失和无效值，再导入 Oracle 的临时检查区。核对无误后进入正式数据。

先搬组织、人员、设备和绑定，再搬健康记录与事件。每批记录来源、导出时间、文件校验信息、成功数、失败数、原因和编号对应关系；失败部分可以单独重做。

旧事件保留原意和来源，不能把过去没有记录的动作补成“已经做过”。新系统日常运行只连接 Oracle。

## 11. E0–E8：每一批做什么，如何验收

### E0：先让开发环境能稳定运行

准备 Java 21、固定构建工具版本、Oracle 开发测试环境，以及开发、测试、正式运行各自的配置。每次修改后自动检查程序能否构建和测试。

做完标准：换一套干净环境，照启动说明能运行；基础测试和页面构建稳定通过。

### E1：能登录、有权限、能保存基础数据

接入 Oracle 和带版本的建表脚本，完成账号、角色、查看范围、输入检查、错误提示、查询翻页和操作记录。页面与后台对需要交换的数据有统一说明。

做完标准：空库可以初始化；正常登录、拒绝越权、分页查询和重要操作留记录都能实际验证。

### E2：管好人员、部门、工种和手表

做好登记、搜索、筛选、翻页、绑定、解绑和导入检查。保存设备绑定历史，准备可重复使用的测试数据。

做完标准：从人能找到当前设备，从设备能查到历次绑定；修改操作有记录，错误导入有明确提示。

### E3：收到并保存真实健康数据

接设备通信，保存原始消息、有效读数和各指标最新值，处理重复、无效和乱序数据，显示在线与读数是否过旧。

做完标准：真实设备或明确标明的协议回放能稳定产生可查询的心率和血氧；重复与无效数据按规则处理，初步设备容量测试有结果。

### E4：让异常事件能够完整处理

完成规则版本、持续异常与恢复判断，支持确认、分派、处理、关闭和误报，保存处理时限、操作经过及待通知记录。

做完标准：连续异常、恢复观察和人工处理都能实际演示。页面刷新后状态仍在，每步操作都可追查。对应总体计划阶段 5。

### E5：统一查询、数字和自动更新

完善实时人员列表、总体摘要、小时与日汇总、个人历史趋势、实时通知与断线补发。为查询速度设定可检查的目标。第一条流程需要的最小查询和更新随 E3–E4 接入，这一批补齐完整能力。

做完标准：各处人数、异常人数、事件数、覆盖人数和数据时间对得上；总览查询不会每次翻遍所有历史记录。

### E6：接入真实地图和位置

保存地图版本、区域和图层，接当前位置及轨迹，支持按区域查询并显示位置是否过旧。

做完标准：地图上任意位置都可追查人员、设备、坐标、来源和时间；没有位置时如实显示。对应总体计划阶段 6。

### E7：补齐扩展功能

实现入井检查、报告、设备控制、原始消息查询、AI 和外部通知，按产品版本分批接页面。

做完标准：每项功能都有权限和操作记录，失败与未配置状态正确，设备或外部系统实际没执行时不能提示成功。对应总体计划阶段 7–8。

### E8：准备上线并演练故障恢复

进行容量、速度、权限和故障测试，试搬旧数据并对账，演练备份恢复、更新失败后的退回和运行告警。

做完标准：主要流程全部通过；正式账号和密码按部署要求配置；备份能恢复，数据搬迁可核对，故障和更新失败都有经过演练的处理办法。对应总体计划阶段 9。

## 12. 哪些能力必须在第一版具备

运行环境、Oracle、数据库升级脚本和自动检查；正式账号权限与操作记录；组织、人员、设备和绑定；有效心率、血氧、历史记录和最新状态；基础异常规则及处理全过程；实时查询、历史查询、总体统计、更新通知和断线补发；基本监控、备份与恢复。

完整地图位置服务按产品计划推进，第一条后台流程不依赖位置必须已经接通。AI、高级趋势、完整三维矿井、全量历史导入、全部设备命令和大型消息系统都可以后续完成。

## 13. 做完后要检查哪些情况

- 正常使用：登录、查询、改资料、绑定设备、触发事件和处理结果从头到尾走一遍。
- 数据异常：重复、乱序、无效、未佩戴、断报和换人佩戴都符合约定。
- 权限：不能看别人的授权范围，不能通过直接请求绕过限制。
- 数据库升级：空环境可以建好，已有环境可以升级，Oracle 查询和分页正确。
- 历史导入：数量、时间范围和抽样内容能对上，失败数据能重做。
- 页面：构建和类型检查通过，登录跳转与数据请求正确，正式环境不能混入演示数据。
- 容量：测试 1000 台设备的基础负载，评估 5000 台设备时的表现，检查查询和空间定位速度。
- 故障：数据库慢、短暂停机、通知断开和重复消息压力下，失败可见且能按约定恢复。

这些是验收工作，不是当前已经确认的故障清单。具体允许延迟、可承受积压、通知保留时间和恢复目标，要在相关阶段先定指标，再测试。

## 14. 上线以后怎样保护数据和发现故障

正式环境不使用开发默认账号。健康信息和联系方式按权限查看，列表默认隐藏部分敏感内容；明文查看和重要操作留记录。日志不写密码、登录凭证或完整隐私数据。

报告注明数据范围、截止时间和用途。AI 提供辅助分析，不代替医学诊断。大文件通过授权入口访问，保存多久、何时删除和归档按上线要求确定。

系统要能查看设备连接、消息解析失败、保存失败、积压、读数过旧、事件产生与处理、通知失败、查询变慢和磁盘增长。日志需要轮换保存，异常达到约定条件要告警。

Oracle 使用经过验证的备份和恢复办法；地图、模型、报告和附件也要有版本和保留策略。备份必须真的试着恢复一次，程序和数据库更新失败时也要有可执行的恢复步骤。

## 15. 下一批的具体工作

1. 确认 Java 21、Oracle 目标环境和连接方式，配好启动与自动检查。
2. 数据库访问固定使用 MyBatis，验证 Oracle 连接、分页、类型映射和 Flyway 升级脚本，不同时维护另一套访问方案。
3. 建好账号、权限、操作记录、人员、设备和绑定所需的数据结构。
4. 把开发用内存账号限制在开发环境，正式认证使用数据库保存的用户。
5. 做好统一查询、输入错误提示和便于排查的请求编号。
6. 完成人员、设备和绑定的数据接口及检查。
7. 整理真实手表通信样本，用同一批样本反复验证解析。
8. 接上心率、血氧、历史保存、最新状态和数据时间。
9. 按确定的规则产生异常事件，支持人工处理并保存全过程。
10. 接入对应页面，完整演示第 1 节的流程，并保存验收结果，再扩展更多指标和页面。

## 16. 第一批页面和后台按同一份约定接起来

以下是要实现的约定，不是已有接口清单。开发时生成对应的类型和接口说明，前端演示数据与真实接口使用相同字段和动作；改变约定要同时更新页面材料、服务端和检查例子。

### 16.1 开始每一批需要什么

D0–D2 需要选定设计图、页面材料、现有前端代码和可运行的开发环境；D3 还需要 Gemini 样板及其交接材料。可以先用集中管理的虚构数据，不需要等待 Oracle 或真实手表。Grok 接手时必须保留样板的字段和状态约定，不得把 mock 数据直接当生产数据。

E0–E1 / D4 接数据库前需要：Oracle 版本与运行方式、可用开发测试实例、连接地址及服务名、测试账号权限。开发者先检查机器已有配置；缺少时由用户或部署负责人提供，密码使用环境变量或本机秘密配置，不写进文档和提交。未取得时可继续页面和独立业务规则测试，不能将其他数据库测试冒充 Oracle 验收。

E3 接手表前需要：设备型号、协议说明、真实或已验证的收发样本、能区分模拟器与真实设备的编号、佩戴及未佩戴样本。无法接真机时可先做明确标记的回放测试，记录尚未真机验收。

E4 发布正式健康规则前需要：业务方认可的指标阈值、持续时间、恢复条件和适用范围。开发者可以从旧系统读取现有配置并整理待确认版本，不能把未经核实的旧值直接当成新系统正式规则。缺少时用测试专用规则检查程序，正式规则保持未配置。

### 16.2 第一版事件动作

体征情况与处理状态使用两个字段，不能互相覆盖。体征情况为异常、恢复观察、恢复、无法判断；设备和趋势事件不适用的恢复字段显示“不适用”。缺失数据不能自动标记恢复。

处理状态为新建、已确认、已分派、处理中、处理完成、已关闭、误报。新事件可以确认，也可以直接分派；直接分派表示接受任务但不补造独立的“确认”操作记录。已确认或已分派事件可以开始处理；未分派时先选择自己为处理人并保存分派记录。

新建、已确认、已分派和处理中可以重新分派，记录原人与新负责人，处理中重新分派仍保持处理中。处理中必须填写措施与结果才能完成；完成后按关闭规则关闭。新建到处理完成的非终结事件可由有权限的人填写理由并标记误报；已关闭与误报只读，第一版不支持重新打开。

关闭健康阈值事件时要求处理完成、有效数据已满足对应规则的恢复条件，并填写关闭说明；数据过旧、无法判断或仍在异常时保留待办并说明不能关闭的原因。设备主动报警和趋势事件要求处理完成且有核实依据与关闭说明，不伪造一个健康恢复状态。

同一人员、同一指标与适用规则形成的一次连续异常只保留一个异常过程。恢复条件满足后再次触发，建立新事件并关联前次过程，旧事件及其未完处置保留。人工误报只终结当前处置，不抹掉健康记录，也不停止规则监测；同一尚未结束的异常过程不因下一条消息立即重复建事件，后续数据继续关联原过程。严重度升级按规则记录变化并让值班人员可见，不能把误报当作永久忽略该人员。

查看、确认、分派、开始处理、完成、关闭和误报分别做服务器权限检查；接口返回当前用户允许的操作清单。页面隐藏按钮仅为使用方便，不承担最终授权。

### 16.3 首批查询和写入

复用当前认证方式：Cookie 保存 User-Token，请求头使用 satoken，响应外层保持 `{code,message,data}`。页面仍通过 `web/src/utils/request.ts` 访问 `/dev-api`，开发代理进入后端 `/api`。新增业务路径先在接口说明中建立，再实现；不假定已有接口。

- `GET /incidents`：按人员、部门、来源、严重度、处理状态、时间、我的待办/未分派/超时等条件查询，返回当前页、每页条数、总数和事件列表。
- `GET /incidents/summary`：使用与列表相同的授权范围和基础筛选，返回总待办、我的待办、高危未确认、未分派、已超时及统计时间。点击摘要后追加对应名单条件，不能从当前页计算摘要。
- `GET /incidents/{incidentId}`：返回事件信息、体征证据、人员设备、处理经过、当前记录版本和允许动作。
- `GET /incidents/assignees`：返回当前用户可以分派到的账号，支持搜索与分页，不暴露无权限部门账号。
- `POST /incidents/{incidentId}/actions`：提交动作类型、所见记录版本、请求唯一编号、目标处理人或措施/结果/原因。服务端检查动作、权限与版本，保存事件、处理经过和待发通知。

人员简要信息可随详情返回；人员完整档案和设备管理分别在 E2 建立接口，不能由该详情接口扩大查询权限。

事件字段至少包括 incidentId、employeeId、employeeName、departmentId、departmentName、deviceId、location 及缺失原因、source、eventCode、severity、occurredAt、evidence、recoveryState、handlingState、assignee、dueAt、version、allowedActions。证据中的每个指标包含值、单位、measuredAt、有效性和过旧标记；时间线每项包含动作、执行人、时间、备注和结果。

编号按字符串交换，时间使用带时区的标准字符串；服务器内部统一时间基准，页面按 Asia/Shanghai 显示。允许为空的设备、位置、负责人和截止时间返回明确空值与原因；严重度排序由接口规定，不按中文标签拼音排序。

查询排序按严重度降序、发生时间升序、事件编号稳定排序。默认每页 20 条，允许 50 条；服务端限制最大页大小，确保前端和后端都不全量拉取后分页。

无权限返回明确拒绝；记录不存在返回未找到；版本过期或不允许的状态转换返回冲突；输入缺字段返回字段错误；依赖不可用返回服务失败。使用相应 HTTP 状态（401/403/404/409/400/503）并保留统一响应说明，不能全部伪装 HTTP 成功。发生冲突后重新查详情，保留尚未提交的备注供用户核对，不能自动覆盖提交。

同一个动作请求编号重试时返回已保存的结果，不新增第二条动作。事件版本检查、动作写入和待通知记录在同一次可靠保存中完成。成功后页面重新查询详情、列表和摘要；关闭事件导致最后一页为空时自动回到仍有效的上一页。

### 16.4 数据异常与实时更新的实施要求

设备带可靠消息编号时用设备与消息编号识别重复；无编号时保留原始消息、解析结果和接收顺序，结合测量时间与内容标记疑似重复，不用“读数相同”直接丢弃合法测量。无法确定设备测量时间时明确标记时间来源，不把服务器接收时间伪装成测量时间。

晚到的数据保存到历史，并按测量发生时的绑定关系归人；归属无法确定时进入待核查记录，不猜人员。旧数据不能覆盖时间更新的当前读数。规则计算处理乱序与重复时不能回拨已经确认的过程，也不能凭补传历史消息制造当前实时报警。

断报后显示读数过旧或无法判断；恢复观察不能靠没有新数据继续计时到“已恢复”。新有效数据到来后重新按规则检查。每项指标独立更新时间。

样板阶段用固定时钟验证通知与过期；真实接口阶段采用服务器提供的数据时间。页面默认 30 秒刷新，手动刷新与自动刷新不并发；关闭页面停止刷新，收到通知时合并短时间内的重复刷新请求。

E3 容量试验先用 1000 台设备和协议约定的发送频率，保存实际频率与数据量；E5 检查页面查询和通知延迟的分布。测试目标初稿是常用查询 95% 在 1 秒内返回、已保存事件在正常网络下 5 秒内让在线页面可见；这是待验证的验收目标，不是性能承诺，测试硬件、数据范围与负载必须同时记录。

数据库暂时不可用时，E3 必须实现有容量上限的可靠缓冲或明确拒收/失败处理，并演练短暂停机后的重放；不能用无限内存保存。先记录设备能否重传、实际峰值写入和允许停机时间，再计算缓冲容量并由部署方确认。通知历史保留时间、缓冲容量与恢复目标属于上线环境参数，在进入真实容量验收前填入配置记录；未定义或未通过恢复试验时不能进入 E8 上线通过状态。

### 16.5 从样板切到真实数据时的检查

页面使用统一的数据访问层，演示与真实接口提供同一份字段和错误结构。Gemini 样板和 Grok 的工程化阶段都可以使用集中管理的 mock adapter；演示功能只在明确的开发模式打开，正式运行缺接口就显示失败或未接入，不静默切回演示数据。计数、时间、按钮权限和处理状态以真实响应覆盖样板模拟。

按页面计划 G 节重新验证所有操作。额外检查 Oracle 保存结果、服务重启后仍在、无权限请求被拒绝、两个窗口产生版本冲突、重复请求只保存一次，以及前端摘要和数据库查询对得上。

页面的 acceptance.json 写明接口版本、数据来源、测试设备或回放方式、执行时间、测试结果与相关截图。D4 通过后才将本页标记为真实业务完成。

## 开发参考：需要写代码时再展开

这些名字保留给开发人员查阅，不需要先懂它们才能理解上面的计划。

<details>
<summary>程序分工和需要保存的数据名称</summary>

## 4. 目标工程结构

后端继续使用当前 `api` 工程，按业务域组织：

```text
com.xzkj.healthnext
├── auth            # 登录、会话、用户、角色、权限和数据范围
├── organization    # 部门、工种和组织关系
├── employee        # 人员档案与在职状态
├── device          # 设备、绑定、在线状态和控制任务
├── telemetry       # 协议适配、原始报文、健康测量和当前状态
├── incident        # 风险规则、风险事件、状态机和处置审计
├── mine            # 地图版本、空间要素、区域和人员位置
├── reporting       # 汇总、报告任务和导出
├── integration     # 外部通知、对象存储和 AI 提供方
└── shared          # 错误码、时间、分页、审计和基础设施
```

模块之间通过明确的应用服务和 DTO 协作，不直接跨模块访问 Mapper 或修改对方的数据表。

## 5. 核心数据模型

第一版迁移至少包含以下数据组。

### 5.1 认证和组织

- `sys_user`
- `sys_role`
- `sys_permission`
- `sys_user_role`
- `sys_role_permission`
- `department`
- `job_type`
- `audit_log`

### 5.2 人员和设备

- `employee`
- `device`
- `device_binding`
- `device_status_current`
- `device_command`
- `device_command_event`

设备绑定必须保留生效和失效时间，不能只在设备表上保存当前人员。

### 5.3 健康数据

- `raw_device_message`
- `health_measurement`
- `person_health_current`
- `health_daily_summary`

`health_measurement` 保存 `measured_at` 并建立时间查询索引；在目标 Oracle 版本与部署条件确认后确定时间分区策略，第一条业务链不以分区为前提。原始报文、接收时间、设备测量时间、服务器入库时间分别保存，不能用服务器当前时间覆盖设备采集时间。

`person_health_current` 按人员保存各指标的最新有效值和逐指标采集时间。某一指标的新数据不能覆盖其他指标的最新值。

### 5.4 风险和处置

- `risk_rule`
- `risk_rule_version`
- `incident`
- `incident_action`
- `incident_assignment`
- `notification_delivery`
- `outbox_event`

所有新增事件使用稳定的 `incident_id`。旧系统的 `warningId + occurredAt` 只在离线迁移映射中使用，不延续为新系统主键。

### 5.5 地图和位置

- `mine_map_version`
- `mine_layer`
- `mine_feature`
- `mine_area`
- `person_location_current`
- `location_history`

空间字段计划使用 Oracle Spatial 的 `SDO_GEOMETRY` 并记录坐标参考系；空间元数据、索引与查询须在目标 Oracle 环境验证。没有坐标系、采集时间或数据来源的位置不得进入当前权威位置表。


</details>

<details>
<summary>技术用语对照与实施检查</summary>

- Flyway：保存并按版本执行数据库建表和升级脚本。
- MyBatis：已选定的 Java 数据库访问工具，Oracle 驱动、分页和类型映射在 E1 验证。
- Netty：接收和解释设备通信的基础工具；具体协议解析与健康规则分开编写。
- 幂等：同一请求或消息重试时，不重复产生业务结果。
- 乐观锁或版本字段：保存时检查数据有没有被别人先修改，避免互相覆盖。
- DTO、OpenAPI、Validation、traceId：分别用于约定交换的数据、记录接口说明、检查输入、追查一次请求。
- Outbox：先把需要发送的通知和业务结果可靠保存，再发送；失败可以重试。
- SSE：服务器向页面发送更新提示。只有确实需要双向即时通信时才使用 WebSocket。
- Last-Event-ID：重连时告诉服务器上次收到哪里；还需实现事件保存、补发和超期重新同步。
- Testcontainers：环境支持时自动启动隔离测试数据库；Oracle 目标版本仍需单独验收。
- RMAN：Oracle 的备份恢复工具，配合归档重做日志制定并演练恢复方案。
- Oracle Spatial / SDO_GEOMETRY：用于保存地图几何和做空间查询，目标环境需验证元数据、索引和坐标。
- 领域测试、状态机属性测试、接口契约测试：分别检查业务规则、不同操作顺序是否破坏规则、页面与服务器交换的数据是否一致。
- 历史查询检查索引效果；使用时间分区后，检查查询是否只读取相关时间段。

Oracle 参考：[空间数据](https://docs.oracle.com/en/database/oracle/oracle-database/21/spatl/spatial-datatypes-metadata.html)、[备份恢复](https://docs.oracle.com/en/database/oracle/oracle-database/21/bradv/backing-up-database.html)。链接用于说明方案依据，目标版本以实际部署环境为准。

</details>
